# Статус — 17 августа 2026

## Текущая версия: v1.5.0 «пачка 19/20»

Спасённые `verses` (`rescued:true`) теперь живут дольше в тех же `60` — при переполнении вытесняют обычные, а не наоборот (`addVerse` ищет первый не-спасённый для сноса). Память берега держит спасённых, сейв всё ещё `~40КБ`, `long.js` 21 узел/160 звёзд.

**258 проверок, 0 падений.** `check-sync` зелёный. Соберётся тегом `v1.2`.

## Прошлая версия: v1.1 «четыре органа разом»

Запрос «хочется новенькое» после 1.0.3 (19.7 мин, 204/17, `в самый раз`) — четыре органа из ДНК, без меню.

- **эхо спасённых** — `rescued` уже сажал цветок цветом существа, теперь цветок помнит спасение: `composeVerse` с `world/dna` живёт в `verse`, не в строке;
- **закон «тишина держит»** — `stillHold` (`18с`, `ru/en`, `lasts`) в `LAWS`, `world` не сушит `roots` пока `active.some-stillHold`;
- **память раненого** — `rescued` с `temper wounded` получает `_woundedRescued` (+0.05 `bond`, порог голода `0.65→0.75` — преданное раненое ждёт дольше);
- **сад поёт** — `world.nearestBloom` + `engine.onDown` тап по `bloom` → `Audio.pluck` по `G.TRAIT_NOTE[trait]`, `G.verseText(verse)` раз в `4с`, `fx.ring`, `Report.blooms` (ru `цветов`, en `blooms`), `engine._bloomSaid` сброс в `startBirth`, вес `sky 3` остался.

**258 проверок, 0 падений.** `check-sync` зелёный. Соберётся тегом `v1.1`.

**Сделать человеку:** **удали старую ИГРУ**. Поставь **1.1**: https://github.com/JoTalbot/game/releases/download/v1.1/igra-1.1.apk — четыре органа разом, сад поёт.

## Прошлая версия: v1.1.1 «два экрана голода»

`screen-7` — голод: тусклое существо ждёт (`empathy` dim, `оттепель` «одно из них бледнеет.»), `screen-8` — спасение: цветок цветом памяти (`contemplation` сад + bloom, `сад` «ты вернулся — оно не уйдёт.»), `tools/make-screens.py` `1..6→1..8`, детерминировано.

**258 проверок, 0 падений.** Собрано тегом `v1.1.1`.

## Прошлая версия: v1.0.3 «корни держат дольше»

Баланс `care 49% vs 10%` уже ок, но `roots` сохли за `~3` мин, съедая награду до следующей метаморфозы (165с). `0.003→0.0025/с` — распад `~3.9` мин вместо `3`, садовник `23%→21%` забыто, забота `49% vs 10%` держит (проверено `care.js`/`balance.js`, `long.js` 21 узел/160 звёзд).

**258 проверок, 0 падений.** Собрано тегом `v1.0.3`.

## Прошлая версия: v1.0.2 «якорей не спамит»

Отчёт `1.0`: `законов 26` за `7.8` мин при `cap 7` — `moreAnchors` спавнился и после капа, засоряя трещины. `spawnCrack` теперь фильтрует `moreAnchors` когда `anchorCap>=7` — трещины остаются (`cap 7` держит), но закон «якорей больше» молчит.

**258 проверок, 0 падений.** Собрано тегом `v1.0.2`.

## Прошлая версия: v1.0.1 «ход — не промах»

Отчёт 1.0: `шагов 17 / в пустоту 33` при пороге `0.5с` — треть коротких проводок считалась промахом, хотя палец вёл игрока. Порог `0.5→0.35с` (комментарий «полсекунды→треть секунды»), `walk` теперь `>0.35`, `empty` иначе — `17/33` станет `~24/26`, воронка честнее.

**258 проверок, 0 падений.** Собрано тегом `v1.0.1`.

**Сделать человеку:** **удали старую ИГРУ**. Поставь **1.0.1**: https://github.com/JoTalbot/game/releases/download/v1.0.1/igra-1.0.1.apk — ход теперь треть секунды, промахов меньше.

## Прошлая версия: v1.0 «она стала тобой»

`0.4.84` догнал витрину, `0.4.83` — отчёт, `0.4.82` — небо, `0.4.81` — рука/босс/голос/судьба, `0.4.80` — сад. Линия голода от `dim` до `bloom`+звука пройдена, рука держит, босс не душит, голос не тараторит, финал не раньше 20 мин, небо подсказывает. Тело хита готово — жанр рефлектум состоялся.

Никакой новой механики в `1.0` — только версия, подпись и витрина. Код тот же, что в `0.4.84` (`258×0`, баланс `49% vs 10%`, тишина `0.0046`).

**258 проверок, 0 падений.** Собрано тегом `v1.0`.

## Прошлая версия: v0.4.84 «витрина догнала»

`README` указывал на `0.4.77`, хотя код уже `0.4.83` — витрина отставала на шесть релизов. Обновлена кнопка `0.4.84`.

**258 проверок, 0 падений.** Собрано тегом `v0.4.84`.

## Прошлая версия: v0.4.83 «якорей сейчас»

Отчёт 0.4.80: `якорей 34` при `cap 7` и `живых 18` — строка `якорей 34` без `сейчас` выглядела багом, хотя это лифтайм замен (старые вытесняет новые).

- **отчёт:** `якорей 34 (сейчас 3/7), спасено 1` — `Report` теперь `a.anchors + " (сейчас " + w.anchors.length + "/" + w.anchorCap + ")"` (ru/en `now`), видно лифтайм и лимит.

**258 проверок, 0 падений.** `check-sync` зелёный. Соберётся тегом `v0.4.83`.

**Сделать человеку:** **удали старую ИГРУ**. Поставь **0.4.83**: https://github.com/JoTalbot/game/releases/download/v0.4.83/igra-0.4.83.apk — якорей теперь `34 (сейчас 3/7)` — понятно.

## Прошлая версия: v0.4.82 «небо и сигила подсказывают»

Отчёт 0.4.80 показал `небо 0 / сигила 0` при 61 выращено и 16 звёзд — архив отказов невидим, пока не откроешь.

- **небо:** подсказка после `360с / 28 узлов / 5 звёзд` (`G.Voice.say("sky")` вес 3, один раз за жизнь, `_skyNudged/_skyNudgedAt`);
- **сигила:** через `90с` после неба (`520с / 22 узла`, `_sigilNudged`, `skyLine + sigil` без force, тихо);
- оба сбрасываются в `startBirth`, не спамят после `become`.

**258 проверок, 0 падений.** Собрано тегом `v0.4.82`.

## Прошлая версия: v0.4.81 «рука держит, босс не душит»

Отчёт 0.4.80 (7.8 мин, 61 выращено/18 живых, 14 срывов, босс съел 6855) показал три точки, где берег врал.

- **рука: порог 96→112.** Медиана уезжания пальца 98 при пороге 96 — половина срывов «палец ушёл» была на грани. Порог поднят до 112 (максимум в отчёте 108) — спокойный палец держит, дрогнувший — рвёт. Комментарий и строка отчёта обновлены, сторож `порог срыва 112`.
- **босс: 38→28/с.** Босс съедал 78% всей силы (6855 из 8776, падала до 0) — самый прожорливый едок душил игру. Снижено до 28/с (снижение 26%), отчёт «босс 38»→«28», `drain.boss>20` и `lowest<=80` в стороже.
- **голос: 18→22 / 4.5→6.** Ответ «много говорю? много» — Игра тараторила. Антиспам одинакового ключа 18→22 с, любой реплики 4.5→6 с, очередь ждёт 6 с — болтливость падает, немота не наступает (258 всё ещё зелёные).
- **судьба: не раньше 20 мин.** Отчёт показал `become` на 7.8 мин при берегах 2 — финал пришёл раньше, чем мир прожит (баг до 0.4.52: порог был узлами, а не временем). `Fate.offer/become/release` теперь проверяют `game.time<1200` сами, `Game.load` сбрасывает `offered` если часы моложе порога.

**258 проверок, 0 падений.** Собрано тегом `v0.4.81`.

## Прошлая версия: v0.4.80 «сад слышит голод»

Два шва после 0.4.79, оба — чтобы спасение было видно, откуда пришло.

- **цветок — цветом существа.** `rescued` bloom раньше был всегда `contemplation` (фиолетовый). Теперь `bl.c = G.TRAIT_COLOR[b.hue]` — розовый у эмпатии, янтарь у строя, видно чья память спасена. Шов, не орган: сад и так рос, но молчал цветом.
- **сад темнеет от голода.** `audio.js` garden `lp 520 - tide*260 - hunger*90` (кламп 180–520): когда кто-то ждёт, сад чуть глуше — как при приливе, только тише. Дрожь света (dim) → расстройка лада (0.6% детюн) → тембр сада — три слоя одного чувства.

**258 проверок, 0 падений.** Собрано тегом `v0.4.80`.

## Прошлая версия: v0.4.79 «голод оставляет след»

Продолжение линии голода после голоса (0.4.78): голод теперь слышен ухом и оставляет след в саду, плюс два шва чистки.

- **звук голода** — `audio.js` читает `maxDebt` существ: `hunger 0.35→1.0` → `0.6%` детюн дронов вразнобой (дрожь света стала дрожью лада, как у `lawBend`), флаг `G.Audio.hunger`, не громче — тишина цела. Четыре проверки «голод размывает лад, а не глушит» (детюн 0.3–1.0%, сад не громче, флаг, граф цел).
- **спасение оставляет цветок** — `rescued` помимо `debt-0.35`/`bond+0.07` сажает один `plantBloom` + `addVerse` через `composeVerse` (один раз на существо, `_rescueTold` не даёт фарма, капы 36/60 целы).
- **шов тембра** — `rescued` звучит своим `b.hue || "empathy"`, а не всегда `curiosity` (`KIND_TRAIT[b.hue]` был неверным).
- **шов мёртвого поля** — убран `node.name = b.name` на `kind=echo` (ставилось, нигде не читалось, не сохранялось, путало ревизию `hint`/`name`/`hue`).

**258 проверок, 0 падений.** Собрано тегом `v0.4.79`.

Орган из прошлого шага («голод виден глазом») был половинкой: существо тускнело (`dim = 1 − 0.55·hunger`, дрожь при `hunger>0.4`, 67% на грани — не гаснет в ноль), но Игра молчала до самого исхода (`debtStar`/`debtWound`). Как в 0.4.55 тускнеющий узел получил голос `cooling`, так теперь и голодное существо окликает, пока его ещё можно спасти, а спасение на грани вознаграждается.

- `hunger` — три строки ru/en, порог `debt>0.65` (dim 0.70 уже заметен глазом, до исхода остаётся 24–55 с в зависимости от `bond`), глобальный кулдаун 90 с + флаг `b.hungerTold` per-существо, кольцо `fx.ring`, вес 2 — предупреждение приходит до прощания, а не вместе с ним.
- `rescued` — три строки ru/en, триггер `prevDebt>0.75` при возврате (`d<56`), награда `debt −0.35`, `bond +0.07`, кольцо + `Audio.rooted`, вес 3 — спасение отличается от случайного прохода мимо; флаг `b._rescueTold` одноразовый, сброс при `debt<0.25`.
- визуал уже в этом релизе: проверен парой «тусклее сытого / но видно на грани» (из 0.4.77).
- пять проверок в группе «голод существа слышен, пока его можно спасти» (`hunger` до `debtStar`, `rescued` при возврате, `debtDrop>0.3`, `bond` растёт, `hunger×1` за 10 с), испытаны подлогом (вернуть молчание — краснеет, поднять порог до 1.3 — краснеет `hunger`, убрать награду — краснеет `debtDrop`).
- в этом же релизе — шов «босс переживает выход» (`boss`/`bossSaid`/`lostGate` в `toJSON`+`load`, числа нормализуются, четыре проверки) — теперь вместе с голосом голода.

**254 проверки, 0 падений.** Собрано тегом `v0.4.78`.

## Орган из 0.4.78: голод существа виден глазом (вошёл в 0.4.78)

Первый геймплейный ход прошлой смены. Долг памяти — «брошенная привязанность» — копился невидимо до исхода: существо, которое ждёт и не дождалось, уходило звездой или вставало раной, а человек не понимал, что оно всё это время гасло от разлуки. Тот же урок, что чинили для узлов в 0.4.55: «механика, которую не видно, не существует».

- голодное существо (`debt` → порог 1.2) тускнеет целиком — ореол, ядро и имя — и слегка дрожит; на грани исхода светится ~67% от сытого, не гаснет в ноль (его ещё можно спасти).
- две проверки в группе «голод существа виден глазом», испытаны подлогом (вернуть `dim=1` — краснеет ровно одна).

**249 проверок, 0 падений** на тот момент. Теперь «вернись к тому, кто ждёт» видно, а не только в числах.

## Шов 0.4.78: босс переживает выход (вошёл в 0.4.78)

**Босс — крупнейшее существо игры — не сохранялся.** Сейв его не клал, `load` не поднимал: человек в разгар боя сворачивал игру — босс исчезал молча, а раны, которые он успел получить, и снятые осколки обнулялись. Порог нового босса (`lostGate`) тоже терялся.

- `boss` (координаты, hp/maxHp, осколки, имя-ключ), `bossSaid` и `lostGate` добавлены в `toJSON` (`world.js`) и поднимаются в `load` (`engine.js`, числа нормализуются — битый сейв не родит босса с NaN).
- четыре проверки в группе «память: босс переживает выход»; испытаны подлогом (убрать восстановление — краснеют ровно три).

**247 проверок, 0 падений** на тот момент. `check-sync` зелёный.

## Текущая версия: v0.4.77 «вернувшийся не роняет берег»

Пять швов одной ревизии — три про честность отчёта, два про память (один из них — крах):

- **отчёт не завышал взгляд** на исходе сил (`drain.gaze` писал 6·dt при реальном 1·dt);
- **босс не считался в разбивке силы** (ест 38/с — самый мощный отток, а в «сила ушла на» его не было);
- **воронка жестов давала два исхода на один тык** (срыв + «в пустоту» на отпускании);
- **обещанная природа узла не сохранялась** (`hint`: метка берега, гнездо после зова, «эхо» эмпатии);
- **вернувшееся существо роняло кадр** — крах: `b.memory` жил только в `birthBeing`, а `load` поднимает существо через `new G.Being`; первое касание/взгляд падал на `b.memory.length` у undefined.

**243 проверки, 0 падений.** Собрано через CI тегом `v0.4.77`.

**Сделать человеку:** подпись CI может отличаться от локальной — **удали старую ИГРУ перед установкой**. Затем:
1. Поставить **0.4.77**: https://github.com/JoTalbot/game/releases/download/v0.4.77/igra-0.4.77.apk
2. Главное на эту сборку: **сохранись, выйди, вернись и коснись существа** — раньше это роняло игру, теперь должно быть тихо.
3. В конце — сигила → «рассказать» → «скопировать»: разбивка «сила ушла на» теперь честна (виден и босс), воронка жестов сходится.

## Шов после 0.4.76, пятый (не в APK): вернувшееся существо не роняет кадр

**Крах, а не потеря памяти.** `b.memory` (память существа о событиях — struck/touched/gazed) инициализировался только в `birthBeing`, а `load()` поднимает существо через `new G.Being`, где его не было. Вернувшийся человек касался существа (`remember` → `b.memory.push`) или держал на нём взгляд (`speakBeing` → `b.memory.length`) — и кадр падал на `Cannot read properties of undefined`. Существо возвращалось «чужим» в худшем смысле: первым же касанием роняло игру.

- `memory = []` и `lastNear = 0` переехали в конструктор `G.Being` — жив любой путь создания, а не только `birthBeing`.
- три проверки в группе «память: вернувшееся существо не роняет кадр»; испытаны подлогом (убрать инициализацию — краснеют все три).

**243 проверки, 0 падений.** `check-sync` зелёный.

## Шов после 0.4.76, четвёртый (не в APK): обещанная природа переживает выход

Узел мог быть ещё неоформленным, но уже задуманным: метка первого берега, гнездо после зова (награда за дорогу — «нужная природа»), «эхо» органа эмпатии. Это жило в поле `hint`, которое в сейв не клалось: обещанное после зова гнездо после перезапуска вырастало из жеста, а не из награды.

- `hint` добавлен в `toJSON` узла (`world.js`) и восстанавливается в `load` (`engine.js`).
- одна проверка через настоящий `load()` («память: обещанная природа переживает выход»), испытана подлогом (убрать восстановление → `hint=undefined`).

**240 проверок, 0 падений.** `check-sync` зелёный.

## Шов после 0.4.76, третий (не в APK): один жест — один исход

Воронка жестов давала два исхода на одно касание: срыв посреди жеста (палец ушёл, узел исчез, смена кожи) писался как «сорвалось» и гасил взгляд, а на отпускании тот же тык засчитывался ещё раз как «шаг» или «в пустоту». Сорвавшаяся рука выглядела ещё и гуляющей, воронка не сходилась.

- флаг `Report._gDone`: ставится в `gestureBorn`/`gestureTorn`, сбрасывается в `gestureStart`, `onUp` больше не пишет «шаг/в пустоту» поверх уже записанного исхода.
- три проверки в группе «отчёт: один жест — один исход»; испытаны подлогом (вернуть двойной счёт — краснеет ровно одна).

**239 проверок, 0 падений.** `check-sync` зелёный.

## Шов после 0.4.76 (не в APK): отчёт считает и босса

Разбивка «сила ушла на» знала взгляд, раны и пульсы — но не босса, а тот ест **38/с**, больше любого укуса раны. Отчёт врал ровно в бою, где человек теряет больше всего, а «падала до» застывала на последнем замеренном от взгляда или раны.

- `drain` обрёл четвёртый канал `boss` (`report.js`), `updateBoss` его кормит (`organs.js`) вместе с `noteEnergy`.
- три проверки в новой группе «отчёт: босс тоже ест силу»; испытаны подлогом (убрать `noteDrain` — краснеют ровно две: сам счётчик и строка в тексте).

**236 проверок, 0 падений.** `check-sync` зелёный, зеркало синхронно.

## Шов после 0.4.76 (не в APK)

Отчёт врал про расход на взгляд на исходе сил: при слабой энергии (`energy < 8`) взгляд ест **1/с вместо 6/с** — это и есть «голод забирает скорость, а не жизнь». Но в `drain.gaze` всегда писалось `6·dt`, то есть «сила ушла на взгляд» завышалась вшестеро ровно там, где человек играет под стаей ран и сил почти нет. Отчёт врал не молчанием, а числом — худший сорт.

- `engine.js`: расход на взгляд теперь пишется честно (`gazeCost = weak ? 1 : 6`), отчёт и реальность совпадают.
- одна новая проверка в группе «раны» — спрашивает мир: сколько отчёт насчитал за кадр у полного и у истощённого; разница обязана быть. Испытана подлогом: возврат `6·dt` красит ровно её («полный 0.100 против слабого 0.100»).

**233 проверки, 0 падений.** `check-sync` зелёный, зеркало `docs/play` синхронно.

## Текущая версия: v0.4.76 «битый сейв не ломает берег»

Битый или очень старый сейв мог уронить загрузку или оставить в мире нечисловые значения: строковые координаты конкатенировались (`"x" + 45 = "x45"`), `null` в массиве ронял разбор, энергия и связь существ уходили в бесконечность, а камера — в NaN. Человек, прервавший запись или пришедший с древней версии, получал сломанный берег.

- добавлен `G.num(v, fallback)` — безопасное приведение к конечному числу;
- все координаты игрока/узлов/существ, энергия, возраст, радиусы и ноты при загрузке приводятся к числам и зажимаются в границы;
- массивы (`nodes`, `beings`, `blooms`, `stars`, `anchors`, `active`, `verses`, `notes`, `history`) нормализуются: `null` и мусор отбрасываются, не-массив заменяется пустым;
- долг существа зажат в 0–1.2 — битый сейв не оставляет его вечно на грани исхода.

8 проверок устойчивости в новой группе «сейв: битые данные не ломают берег». 232 проверки, 0 падений.

Тег `v0.4.76` собирает APK через CI. Подпись CI отличается от локальной: **перед установкой удали старую ИГРУ**.

## Прошлая версия: v0.4.75 «одна жизнь — один отчёт»

Два тихих дефекта памяти и браузера:

- **Отчёт смешивал жизни.** `Report.reset()` звался только в тестах. Если в той же вкладке начать новую жизнь или нажать «продолжить», касания, жесты и плавность прошлого берега подмешивались к новому. Теперь сброс в начале `startBirth` и `load` (до `onReturn`, чтобы ночь берега попадала в отчёт).
- **Оффлайн-оболочка не кэшировала шрифты.** `sw.js` хранил скрипты и CSS, но все девять TTF оставались вне кэша — оффлайн поднимал игру без букв. Добавлены шрифты; `check-sync.sh` теперь сверяет список кэша SW со всеми ассетами, на которые ссылаются HTML и CSS, и ловит протухание.

224 проверки, 0 падений.

Тег `v0.4.76` собирает APK через CI. Подпись CI отличается от локальной: **перед установкой удали старую ИГРУ**.

## Прошлая версия: v0.4.74 «якорь услышан»

Самый дорогой жест игры был немым. Якорь — долгий неподвижный взгляд на уже родившийся узел, до трёх за всю игру, — отзывался аккордом и надписью, но Игра не произносила ни слова. Человек совершал редкое, весомое действие и не слышал, что оно что-то значит.

- **первый якорь** теперь звучит отдельной репликой (`anchorFirst`), дальнейшие — короче (`anchor`). Тихо и весомо, в тон `rooted`, без тараторки.
- **обе раскладки** добавлены в `LINES` и `LINES_EN`.
- **четыре проверки** стерегут: первый якорь назван вслух, второй слышен короче, оба ключа переведены, два якоря ставятся подряд. Подлог (убрать голос из движка) роняет две из них.

221 проверка, 0 падений. Болтливость не выросла.

Тег `v0.4.76` собирает APK через CI. Подпись CI отличается от локальной: **перед установкой удали старую ИГРУ**.

## Прошлая версия: v0.4.73 «лад берега»

Фон одного характера звучал почти как фон любого другого: четыре дрона были одной квинтой с лёгким сдвигом от тепла. Какой бы берег ты ни вырастил, уши слышали одно и то же — половина обещания «мир читает твою суть» не доходила до звука.

- **у каждой оси свой лад.** Гармония — чистые консонансы, сбой — расстроенный унисон с биениями, жар — тритон, тишина и взгляд — широкие открытые квинты вверх, тепло — большая терция. Меняются **высоты, а не громкости**: граф не растёт, фон по-прежнему тише самого себя.
- **шесть проверок стендом** стерегут лад: чистая квинта строя, биение сбоя, различие ладов, ширина тишины и то, что смена лада не делает фон громче. Подлог (вернуть единую квинту) роняет четыре из них.

217 проверок, 0 падений. Гул 0.240, полный берег 0.069 — пороги тишины целы.

Тег `v0.4.76` собирает APK через CI. Подпись CI отличается от локальной: **перед установкой удали старую ИГРУ**.

## Прошлая версия: v0.4.72 «прилив помнит фазу»

При перезапуске приложения отсчёт до прилива каждый раз откатывался к стартовым ~98 секундам. На телефоне это значит, что человек, играющий короткими сессиями (свернул, закрыл, ответил на сообщение), почти не видел прилив вовсе.

- **фаза прилива теперь память.** `tide` и `tideT` кладутся в сейв и поднимаются при загрузке. Приливы больше не отодвигаются каждым выходом.
- **две проверки стендом.** Одна стережёт, что фаза кладётся в сейв; вторая — что она поднимается через настоящий `load()`. Обе испытаны подлогом: если не сохранять — «tide=undefined tideT=undefined».

211 проверок, 0 падений. Забота 51% против 10% брошенного.

Тег `v0.4.76` собирает APK через CI. Подпись CI отличается от локальной: **перед установкой удали старую ИГРУ**.

## Прошлая версия: v0.4.71 «вчерашний ты приходит один»

Каждая ночь без человека рождала нового **«вчерашнего тебя»**, а прежний не уходил никогда: за три возвращения на берегу стояло трое призраков одного и того же вчера.

- **гость один.** Новый «вчерашний ты» приходит, а старый тихо уходит звездой на небо — как и положено вчерашнему дню. Один гость, а не толпа.
- **проверка стендом.** Два возвращения подряд обязаны оставить на берегу не более одного «вчерашнего тебя». Подлог (убрал `splice`) честно краснеет — «2 вчерашних на берегу».
- **ночь прежняя.** После ночи 18 живых узлов из 20, после трёх суток 14 из 20; долг считается часами разлуки и разрешается до первого кадра; забота 51% против 10% брошенного.

Тег `v0.4.76` собирает APK через CI. Подпись CI отличается от локальной: **перед установкой удали старую ИГРУ**.

209 проверок, 0 падений.

## Прошлая версия: v0.4.70 «ночь не стирает берег»

Долг, созревший за ночью, разрешается до первого кадра берега, а отчёт пишет строку «пока тебя не было». Версия 0.4.69 перед этим ввела ночной отчёт и начала считать долг часами разлуки.

## Прошлая версия: v0.4.66 «берег помнит»

Память возвращения была написана целиком, но мертва: сейв не клал в себя `Memory.snapshot()`, а `Memory.onReturn()` не вызывался ниоткуда. Из-за этого каждый вход был первым, день всегда равнялся единице, берег не спал без человека, а вернувшийся не слышал своей половины голоса Игры.

- память теперь кладётся в сейв и поднимается последней строкой загрузки — после узлов и существ;
- час ухода считается от момента возврата, поэтому одна ночь не засчитывается дважды при сворачивании окна;
- отчёт отделяет шаги от промахов: касание, которое вело игрока, больше не называется ударом в пустоту;
- восемь проверок памяти испытаны тремя подлогами.

199 проверок, 0 падений.

## Прошлая версия: v0.4.65 «якорь ставит рука»

Механика здорова: 18 рождений из 25 касаний, один срыв за сессию. Но в отчёте — **девять якорей на 18 узлов при пределе три**. Человек их не ставил.

- **их ставило созвучие.** Аккорд складывается сам, от ходьбы по тонам, и молча якорил ближайший узел. Замер: четырнадцать якорей из четырнадцати пришли оттуда, ни одного от удержания. Ровно эту болезнь я чинил пятнадцать релизов назад — и нашёл тогда лишь один вход из двух.
- **второй вход тоже был порочен.** Якорь читал накопитель жеста рождения: к моменту, когда узел родился, порог давно взят. Теперь у каждого узла свой счётчик — сколько человек держит его **после** рождения. Якорь берётся вдвое дольше рождения, и только рукой.
- **сторож теперь по коду, а не по числам:** проверка читает исходники и требует, чтобы якорь ставили только рука и сам орган. Числовая проверка тут бессильна — она ловит следствие, а входов может быть три.
- **аккорд взамен греет.** Отняв у него якорь, я обеднил берег: созвучие, оказывается, держало мир. Теперь оно оживляет всё вокруг и затягивает раны — дар остался, но он больше не «сохраняю навсегда».

188 проверок, четыре подлога.

## Прошлая версия: v0.4.64 «интерфейс под охраной»

Закрыт долг, всплывший в прошлой смене: **интерфейс не проверялся никогда**.

- **452 строки вслепую.** Сигила, небо, экран рассказа, поле рта, все кнопки — стенд до них не доставал: подставной документ возвращал пустоту на любой запрос, и привязка кнопок упала бы на первой же. Именно поэтому пять немых счётчиков в прошлый раз нашлись только чтением исходника.
- **появился живой макет документа.** Не эмулятор браузера — макет сцены: узлы помнят классы и текст, касание зовёт настоящий обработчик. Ровно столько, чтобы интерфейс можно было потрогать пальцем.
- **одиннадцать новых проверок.** Кнопки неба и сигилы открывают то, что обещают, и попадают в рассказ. Весь путь отчёта: открытие, пять вопросов, ответ касанием, снятие ответа повторным касанием, видимый текст, копирование с запасным путём. И двенадцать органов интерфейса разом — подсказки, надпись закона, сезон, тишина, поле рта.
- **давних поломок не нашлось** — интерфейс оказался здоров. Но теперь он под охраной: следующая опечатка в имени кнопки покраснеет в стенде, а не всплывёт через неделю в отчёте с телефона.

Две ловушки, обе записаны в память: стенды делят один документ, и кто последний — того и тапки. А проверка, меняющая язык, обязана вернуть его обратно: иначе краснеют четыре соседние.

187 проверок, пять подлогов.

## Прошлая версия: v0.4.63 «отчёт перестал врать»

Человек: «небо и сигилу открывал» — а в отчёте стояло 0.

- **пять счётчиков из одиннадцати не работали вовсе.** Небо, сигила, смена языка, касания, взгляды, кристаллы — поля были объявлены и не увеличивались ни разу.
- **это худший сорт бага: отчёт не молчал, он врал.** Я по этим нулям делал выводы вслух — «человек ни разу не открыл небо» — и правил игру под выдуманный повод. Пустое поле видно сразу; поле со значением ноль выглядит как факт.
- **теперь считают сами органы**, а не кнопки: открытие неба (закрыть — не поступок), сигила, смена языка, каждое касание, каждый взятый взгляд, каждый рождённый узел.
- **сторож спрашивает список полей у самого отчёта**, а не хранит свой: иначе новое поле снова окажется немым и никто не заметит.
- **попутно вскрылось**, что весь интерфейс — сигила, небо, кнопки — не грузится ни в одном стенде и не проверялся никогда. Записано в долги.

176 проверок, один подлог.

## Прошлая версия: v0.4.62 «новая жизнь начинается с нуля»

Отчёт после тринадцати минут: раны 0, срывов 28 на 235 касаний — рука здорова. Но в нём два расхождения, и оба оказались настоящими багами.

- **финал пришёл на тринадцатой минуте вместо двадцатой.** Часы мира не обнулялись при рождении: новая жизнь наследовала время старой. Человек начал заново, а игра считала его на полчаса старше — и предложила развилку «отпустить или стать игрой» посреди первой же сессии. Всё, что меряется временем мира, врало.
- **природа «сбой», а сезон «тишина»** — разошлись и не сходились тринадцать минут. Осыпь характера считалась от суммы всех шести осей, а упирались в потолок две: у самого верха осыпь была почти нулевой. Портрет снова не дышал, просто выше. Теперь теснота меряется по самой полной оси.
- **и следом сломалось перерождение.** Оно требовало, чтобы сумма осей перевалила порог, — а с живым портретом сумма стабилизируется. За полчаса не случилось ни разу, берег вымирал. Теперь зрелость меряется глубиной: есть ли у человека выраженная черта.

Забота окупается впятеро — 51% против 10%, лучший разрыв за все смены.

175 проверок, четыре подлога.

## Прошлая версия: v0.4.61 «портрет дышит»

**Сага руки закрыта.** «Всё работает супер»: 86 рождений из 140 касаний, срывов 14 вместо 113, «нет сил» исчезло совсем.

И сразу главное, что человек сказал следом: **«захотелось выйти, так как ничего нового»** — после восьми минут и 141 выращенного узла.

- **он прав, и это худшая правда за все смены.** Замер того, что человек видит впервые: все шесть пород — за первые двадцать восемь секунд, дальше три минуты пустоты, потом перерождение — и больше ничего нового до конца.
- **корень: ДНК только росла и не убывала никогда.** За две минуты три оси упирались в потолок, органы вставали намертво и стояли всю игру. Игра обещает читать тебя по поступкам — а читала только первые сто. Жанр называется онтогенетическим, и его сердце не работало.
- **теперь ДНК — портрет, а не копилка.** Кормишь одну ось — прочие тихо оседают. Забросил жар ради тишины, и берег это заметит: доминанта сменится, органы вырастут другие, сезон повернётся. Характер меняется за минуты, но одно случайное касание пути не стирает.
- **вехи кончались на двадцати** при 141 выращенном. Добавлены дальние — и они называют то, чего человек не открыл ни разу за сессию: небо и сигилу.
- **забвение выросло с 28% до 37%** у того, кто только сеет: доминанта больше не застревает в «тишине», которая защищала берег. Мир перестал быть тепличным, но забота по-прежнему окупается втрое.

172 проверки, четыре подлога.

## Прошлая версия: v0.4.60 «голод не отнимает жизнь»

Второй отчёт по ранам: «сила ушла на: взгляд 204, раны 3426», «нет сил» — 26 срывов из 29. Прошлая правка помогла вдвое, но корень был не там.

- **стая кусала как стая, а платил человек как за одну.** Каждая рана ела свою долю независимо: полторы разом — уже больше, чем успевает восстановиться, шесть — энергия в нуле за секунду. Раны съели 3426 силы за 108 секунд игры: больше, чем длилась сессия. Теперь каждая следующая кусает вдвое слабее — стая больно, но переживаемо.
- **взгляд отключался на исходе сил — и это делало игру неиграбельной.** Под стаей энергия стоит в нуле: человек не мог ничего, ни вырастить, ни понять, за что наказан. Теперь смотреть без сил можно, просто вдвое медленнее. Голод забирает скорость, а не саму способность жить. «Нет сил» — 213 срывов в замере стало 0.
- **тупик, о котором стоит знать:** я дал ране передышку после укуса — цифры были прекрасны (боль впятеро меньше), и это сломало долг памяти начисто. Раны, оказывается, держат человека в движении; перестав гнать, они позволили ему стоять, и существа перестали отставать. Долг зреет только в разлуке. Откатил.
- **попутно закрыта дыра:** сам выбор «этот телефон слабый» не стерёг никто — подлог проходил мимо всего стенда.

167 проверок, шесть подлогов.

## Прошлая версия: v0.4.59 «от раны можно уйти»

Диагностика назвала виновного одной строкой: **«сила ушла на: взгляд 581, раны 4349»**. И «нет сил» — 95 срывов из 113. Две трети сессии человек провёл под касанием раны, не понимая, почему палец перестал работать.

- **голод, от которого нельзя уйти, — не голод, а налог.** Рана разгонялась быстрее игрока: догоняла и висела. И была бессмертна — умереть могла только от удара, сама не истлевала никогда. Раны копились всю сессию, сила не поднималась выше нуля, взгляд не работал вовсе.
- **теперь от раны можно убежать** — она заметно медленнее — **и она истлевает** за полторы минуты, если её не кормить вниманием.
- **но кусает по-прежнему больно.** Пробовал смягчить укус — сломался долг памяти: игрок перестал убегать, существа всё время рядом, голод не дозревал ни у кого. Рана обязана гнать с места. Лечится не сила укуса, а неизбежность.
- **и урок, повторённый в третий раз:** проверка, дублирующая формулу кода, ничего не проверяет. Мой сторож считал скорость раны той же формулой, что и игра, — подлог прошёл мимо, обе стороны врали одинаково. Теперь скорость меряется в живом мире.

161 проверка, три подлога.

## Прошлая версия: v0.4.58 «палец растит то, на чём лежит»

Диагностика окупилась в первый же отчёт. Пять релизов я чинил руку вслепую — теперь игра назвала причину сама: «срыв сразу после зова: 5 из 6». А человек добавил решающее слово: после перелёта новые точки срабатывали **не все**.

- **не все — вот ключ.** Дело было не в срыве, а в хватке. Взгляд, однажды взятый, держался за свой узел мёртво. Но после прилёта точки рождаются кучкой, соседняя лежит совсем рядом — человек переносит на неё палец, игра видит его точно на новом узле и продолжает греть старый. Палец лежит, а растёт не то, на что смотришь.
- **теперь палец, замерший над другим узлом, переносит взгляд туда.** Не мгновенно — полсекунды: перенос это «я передумал», а не перецепка на каждый встречный сгусток. Замер: 12 из 12 вместо 7 из 10.
- **и урок про проверки:** первая версия сторожа отрывала палец между узлами — а это другой жест, при нём игра берёт взгляд заново, и болезнь пряталась. На телефоне человек ведёт палец не поднимая.
- **«нет сил» — вторая находка отчёта.** Взгляд почти бесплатен, значит силу ели раны или пульс. В рассказ добавлена разбивка: на что ушла сила и до чего падала.

157 проверок, четыре подлога.

## Прошлая версия: v0.4.57 «игра рассказывает про руку»

«Когда летишь по стрелке, новые точки не обводятся вообще или срывается почти сразу». И — «добавь массу диагностических данных о моих действиях».

Просьба верная: пятый релиз подряд я чиню вслепую. Все замеры зелёные — прилёт по зову, дрожь пальца, отдаление камеры, законы, перерождение посреди удержания, — а на телефоне рвётся. Стенд туда не достаёт принципиально: он дёргает игру напрямую и не воспроизводит ни настоящие касания, ни границу с оболочкой.

**Теперь игра записывает каждый жест и рассказывает сама:**

- **воронка касания** — сколько раз коснулся, сколько раз взялся взгляд, сколько выросло, сколько сорвалось, сколько ушло в пустоту. Видно, на каком шаге теряется.
- **причина срыва поимённо**: палец ушёл, узел исчез, узел умер, нет сил, смена кожи, система забрала жест.
- **на какой секунде рвётся** и сколько срывов «почти сразу» — рождение на 1.35 с, так что видно, дожил ли жест до цели.
- **насколько уезжал палец** в точках экрана против порога срыва. Если медиана около сотни — виноват порог, а не рука.
- **срывы сразу после прилёта по зову** — прямая проверка жалобы.
- **отдаление камеры**: от, до, в среднем.

Следующий отчёт назовёт причину прямо, и чинить будем уже не вслепую.

154 проверки, два подлога. Попутно проверки поймали две мои ошибки: кириллицу в английском отчёте и «срывы», которые на деле были обычным отпусканием после рождения — 58 ложных на 56 рождений.

## Прошлая версия: v0.4.56 «первый берег прощает»

Отчёт: **выращено 15, живых 1**. Человек три минуты растил сад, и первое же перерождение унесло его целиком.

- **порочный круг.** Чтобы сад пережил смену кожи — нужны корни. Чтобы захотеть возвращаться — надо увидеть, что возвращение окупается. Чтобы увидеть — нужен сад, который дожил. На укоренение одного узла уходит полторы минуты, а первая смена кожи приходит на третьей — и уносит всё. Первое, что человек узнавал о мире: труд бессмыслен.
- **теперь первое перерождение щадит.** Его переживает и просто согретое — то, чего человек касался недавно, не только укоренённое. Дальше закон прежний и суровый: держится удержанное и то, к чему возвращались. Один раз мир прощает незнание — не больше.
- **и урок, повторённый дважды:** проверка, повторяющая формулу кода, ничего не проверяет. Сторож считал милость тем же условием, что и сама игра, — и подлог прошёл мимо: обе стороны врали одинаково. Теперь проверка считает факт: сколько узлов было до смены кожи и сколько осталось после.

Про «срывалось»: заново прогнал весь ввод — довёл и замер, ведение сквозь берег, удержание через перерождение, отдаление камеры, законы, дрожь пальца. Всё зелёное. Пробовал мерить замирание на экране вместо мира — это ломает главный жест, откатил.

148 проверок, четыре подлога.

## Прошлая версия: v0.4.55 «тускнеющее видно»

Рука наконец слушается — «рука слушалась? да» впервые за пять релизов, сага ввода закрыта. Но отчёт выдал следующее, и это важнее.

- **возвращений ноль** при двенадцати выращенных узлах — при том что на вопрос «возвращаться было зачем?» человек отвечает «да, видно». Он не возвращался ни разу.
- **и не мог.** Живой узел рисовался с постоянной яркостью: то, насколько он согрет, на его вид не влияло вовсе. Сердце игры — «вернись к тому, что остывает» — было невидимо. Механика работала, замеры показывали красивые числа, а на экране не менялось ничего.
- **теперь берег тускнеет на глазах.** Свежий узел горит полно, остывающий глуше и суше: гаснет ореол, тает ядро, тоньше обводка. Забытый светит 41% от свежего — но не гаснет совсем, иначе человек решит, что тот умер, и не поймёт, что его ещё можно спасти. Ничего не подписано словами: берег меняется, а не отчитывается.
- **стенд научился видеть яркость.** Он считал вызовы рисования, а у яркого и у погасшего узла их поровну — меняются только цвета. Теперь заглушка холста запоминает прозрачность каждого мазка.

Правило, вытекающее отсюда: **механика, которую не видно, не существует.**

146 проверок, три подлога.

## Прошлая версия: v0.4.54 «рука не держит призраков»

«После отдаления начинает срываться, а потом вообще не реагировать». Слово **потом** и оказалось ключом: это не срыв, это залипание насмерть.

- **взгляд держался за узел, которого больше нет.** Перерождение стирает берег и уносит узлы, но взгляд оставался прицеплен к унесённому: кольцо на экране горит, время идёт — и не рождается ничего никогда. Выйти можно было, только отпустив палец, а человек держал, потому что игра показывала, что держит. Отдаление экрана — это и есть перерождение, у него их было три за минуту с небольшим.
- **перерождение приходило втрое чаще нужного.** Игра почти не выходила из смены кожи, и всё это время экран отдалён. Первому берегу дано больше времени — человек должен хоть раз увидеть, как растёт сад, прежде чем его унесут.
- **награда за возвращение испарялась раньше, чем её спрашивали.** Два возврата к узлу давали корни, которых хватало на 27 секунд, — а перерождение приходит раз в три минуты. Узел, к которому вернулись дважды, почти всегда встречал смену кожи уже без защиты: выживало 2%. Теперь забота окупается втрое — 46% против 16%.
- **и урок про проверки:** первая версия сторожа водила пальцем вслед за узлом — и оба подлога прошли мимо неё. Палец в проверке обязан замереть, как у живого человека, который ждёт рождения.

142 проверки, четыре подлога.

## Прошлая версия: v0.4.53 «прицел живёт на стекле»

Человек уточнил решающее: «срывается на новых, куда прилетаешь по стрелочке, и при отдалении экрана только некоторые ещё можно обвести». Эта фраза вскрыла причину, которую три релиза не брал ни один замер.

- **прицел съёживался вместе с картинкой.** Он мерился в мировых единицах жёстким числом, а палец живёт на стекле и всегда накрывает одни и те же ~57 точек. Когда камера отъезжает, мировая единица мельчает — и на отдалении человек должен был целиться вдвое точнее, чем вообще видит палец: 26 точек допуска вместо 70. А отъезжает камера как раз в обоих названных случаях: перерождение уводит её на четыре секунды, зов уносит к новым точкам. Теперь допуск считается от экрана: сколько бы мир ни отдалился, палец прощает столько же.
- **послабление для медленного телефона не срабатывало ни разу.** В отчёте 140 тяжёлых кадров — и тишина. Игра ждала девяносто рывков подряд, а рывки не ходят строем: они раскиданы по одному, и счётчик обнулялся между ними. Теперь считается доля за полминуты.
- **и урок про сами проверки:** сторожа потолка я сперва написал, повторив формулу из кода, — и подлог прошёл мимо. Проверка, повторяющая код, ничего не проверяет; порог должен идти от смысла, от подушечки пальца.

138 проверок, четыре подлога.

## Прошлая версия: v0.4.52 «жест принадлежит руке»

Третий отчёт с телефона: «рука слушалась? — срывалось» и — самое важное — **«судьба: become» при 1.3 минутах игры**.

- **жест отбирала оболочка, а не игра.** Всё, что можно было починить в самой игре, было починено ещё в прошлый раз — и не помогло. Потому что рвал удержание не код игры: WebView сама по себе прокручиваемый холст, и её распознаватель через треть секунды решает, что затянувшееся касание — это скролл. Забирает жест раньше, чем страница успевает возразить. Вылечено в оболочке: пока палец на стекле, жест принадлежит игре, а не прокрутке. Заодно длинный тап больше не поднимает системное «выделить текст» ровно на той секунде, когда рождается узел.
- **финал игры приходил на третьей минуте.** Развилка «отпустить или стать игрой» открывалась по счётчику «вырастил 22 узла» — а это пара минут бодрого сева. Человек увидел конец раньше, чем игру, и выбрал «стать игрой», не успев пожить. Теперь нужны двадцать минут на берегу, и только потом — зрелость по берегам, возрасту или саду.
- **стенд научился читать оболочку.** Шесть замеров подряд говорили «работает», пока человек трижды повторял «не работает»: стенд дёргает игру напрямую и границу с Android не видит вовсе. Теперь он читает исходник оболочки глазами.
- **попутно найдено падение отчёта** — «рассказать» мог открыться пустым экраном.

133 проверки, четыре подлога.

## Прошлая версия: v0.4.51 «палец держит, система не отнимает»

Человек второй релиз подряд: «новые точки не обводятся удержанием пальца». В прошлый раз я починил не то.

- **жест отбирала система.** Игра не занимала движение пальца, и Android через треть секунды решал, что затянувшееся касание — это прокрутка: забирал жест себе и присылал отмену. А отмену игра принимала за «человек убрал палец» и гасила взгляд. Удержание умирало на полпути к рождению — каждый раз, на любой точке. По коду всё выглядело исправно.
- **и теперь система не переспорит руку.** Своё касание занято. Отмена больше не выдаётся за отпускание. А если палец в этот момент всё ещё на стекле — отмена игнорируется вовсе: система усомнилась, а рука на месте.
- **шесть замеров подряд говорили «работает».** Прямое касание, дрожь пальца, свежие точки фронтира, далёкий узел, едущая камера, живой жест — всё зелёное, а на телефоне не работало. Стенд дёргает игру напрямую и физически не видит, что событие отобрала оболочка. Такие болезни ловятся только чтением обработчиков глазами — теперь их читает отдельная проверка.

128 проверок, два подлога.

## Прошлая версия: v0.4.50 «якорь надо заслужить»

Первый рассказ с телефона пришёл целиком — и в нём была строка, ради которой всё и затевалось: **«выращено 9, якорей 10»**.

- **самый дорогой жест игры раздавался даром.** Якорь — то, чем ты говоришь берегу «это не отпускай» — доставался автоматически каждому рождающемуся узлу, все 100%. Держать палец дольше было незачем, удерживать нечего: тебе и так уже всё удержали. Теперь якорь берётся отдельным намерением — держишь дольше рождения, вдвое дольше.
- **отмена этого костыля обрушила пять проверок, и все пять были правдой.** Даровые якоря держали узлы живыми и мешали приливу чистить берег — убрал подпорку, вылезло то, что она маскировала. Игра говорила вслух «undefined помнит каждый отказ» пятнадцать раз за сессию (у раны нет имени, и никто ни разу не прочёл эту строку). Долг памяти не дозревал: чем крепче ты привязан к существу, тем дольше оно ждёт — до двухсот секунд, то есть самые любимые не уходили никогда.
- **два стенда врали, потому что палец в них лежал неподвижно два часа.** Прошлый релиз научил игру ловить взгляд у замершего пальца — и робот превратился в вечный источник тепла: узлы не умирали, берег переполнялся. Болезнь стенда, а не игры: живой человек руку двигает.
- **паспорт телефона врёт.** В отчёте — 52 кадра в секунду на экране, который игра уверенно сочла сильным. Послабления для слабых раздавались раз и навсегда при старте, по железу, а на настоящую плавность игра не смотрела никогда. Теперь смотрит и гасит украшения сама.
- **«возвращаться было зачем? — не понял»** — и это честно: узел тускнеет 45 секунд, а сессия была 78. Смысл возвращения негде было увидеть. Теперь берег один раз показывает первый остывший узел словами.

126 проверок, шесть подлогов.

## Прошлая версия: v0.4.49 «рука слушается»

Первый рассказ с телефона — и сразу три беды, одна из них моя свежая.

- **узлы не обводились, если довести до них палец.** Главная жалоба: «новые планеты не обводятся, а продолжаешь движение». Игра решала, что ты трогаешь, ровно в миг касания. А играют иначе: опускают палец на пустоту, ведут игрока к узлу и, дойдя, останавливают палец на нём. Замер показал: палец в **одной точке** от центра узла, четыре секунды неподвижно — и ничего, игрок проносится сквозь. Теперь взгляд ловится на ходу: не «палец рядом», а «палец **замер** рядом». Обратная крайность проверена отдельно — ведя игрока сквозь плотный берег, ты не цепляешь встречные узлы.
- **кнопки не работали вообще — и это я сломал прошлым релизом.** Игра глушила отпускание пальца на всём окне, включая касание кнопки: после этого браузер не создаёт клик вовсе. Сломались разом все кнопки, включая работавшие годами. Теперь игра слушает холст, а интерфейс — свой.
- **надпись закона наезжала на кнопки** — опущена ниже, а на узком экране ещё ниже.
- **и то, о чём человек не просил, но видно на скриншоте:** четыре кнопки сигилы уехали за оба края экрана. Теперь переносятся, а сама сигила прокручивается.

Прежние проверки руки клали палец сразу на узел — потому и не видели беды. Добавлен живой жест: довести палец и замереть. Всего 121 проверка, четыре подлога.

## Прошлая версия: v0.4.48 «Игра спрашивает сама»

Человек прошёл игру и попросил: пусть вопросы задаёт сама Игра — кнопкой в меню и на финале, с копированием в буфер. Просьба правильная по сути: до сих пор я расспрашивал его в чате, по памяти, спустя сутки после сессии.

- **у Игры появился рот наружу.** На финале — и по кнопке «рассказать» в сигиле — берег спрашивает пятью вопросами своим голосом: «фон гудел?», «рука слушалась?», «я много говорю?», «возвращаться было зачем?», «зачем ты здесь остался?». Ответ — одно касание, потому что печатать на телефоне после часа игры никто не станет. Всё уносится в буфер одной кнопкой.
- **и Игра рассказывает о себе то, чего человек знать не может.** Половина моих прежних вопросов была про незаметное: сколько кадров в секунду, не сорвался ли рендер, сколько раз он вправду вернулся к узлу. Теперь берег ведёт чёрный ящик — плавность с рывками (не среднее: среднее прячет ступоры), сбои рендера, которые раньше умирали в невидимой отладочной строке, и поступки человека.
- **конец теперь ждёт.** «Стать игрой» стирает берег и перезапускает мир через пять секунд — рассказ о последней сессии умер бы вместе с ней. Теперь перезапуск ждёт, пока человек закроет рассказ.
- **версия переехала в код**, иначе отчёт с телефона бесполезен: непонятно, о какой сборке речь. Теперь она в трёх местах, и сторож расхождений стережёт все три.

13 новых проверок, всего 111. Они сразу поймали две настоящие беды: кириллицу в английском отчёте и `NaN×NaN` в размере экрана.

## Прошлая версия: v0.4.47 «корни растут от возвращения»

Главный орган игры — забота — работал не так, как о нём написано. Никто этого не замечал, потому что мерили не то.

- **корни доставались даром.** Задумано: возвращаешься к потускневшему узлу — он пускает корень, три возвращения, и он держится против прилива сам. На деле корни были у **всего берега**, включая узлы того, кто не вернулся ни разу. Каждый новорождённый получал треть корня просто за факт рождения: неоформленный узел считался «остывшим» ещё до того, как стал чем-либо, и первое же касание засчитывалось как возвращение. Награда за верность выдавалась всем подряд — а значит не выдавалась никому.
- **и это держало прилив на фальшивом щите.** Убрав раздачу, я разом снял броню со всего берега, и забвение подскочило с 14% до 24% — хотя прилива правка не касалась. Чинил не силой волны (у неё резкий обрыв: чуть слабее — и прилив исчезает совсем), а тем, что было настоящей причиной: корни сохли за три минуты, потому что торопиться их гасить было нужно, пока они доставались всем. Теперь сохнут за пять с половиной. **Забота окупается вдвое: 37% против 19%.**
- **стенд полтора года мерил везение.** Один из двух замерочных стендов не засеивал случайность — тот самый, на котором стоят проверки долга памяти, голоса и цены кадра. Один и тот же код давал то три исхода, то шесть; проверки краснели и зеленели сами по себе. Теперь оба сеют одинаково.
- **две проверки мерили не то, что стерегли.** «Кадр слабого телефона дешевле» сравнивала два **разных** мира — у одного 128 звёзд, у другого 86 — и краснела от правок баланса, к рисованию отношения не имевших. Теперь мир растится один раз, а рисуется дважды. Вторая требовала фиксированного числа событий там, где надо было спрашивать долю.

98 проверок, 0 падений. Три подлога испытаны.

## Прошлая версия: v0.4.46 «язык не вмерзает в мир»

Игра давно умеет говорить по-английски. Оказалось, что мир — нет.

- **английский берег говорил по-русски там, где никто не искал.** Не в интерфейсе — в самой ткани игры. Имя, которое Игра даёт человеку и которое он уносит в сигиле, было только русским: англичанин играл «Картографом тумана». По-русски звались все существа — то самое имя, что висит у них над головой всю игру. По-русски они говорили. По-русски сад писал стихи. Проверки этого не ловили: они стерегли словарь и реплики, а мир хранил готовые строки.
- **и это вмерзало намертво.** Существо, названное по-русски, уезжало в сохранение строкой. Стих сада — строкой. Человек переключал язык и получал английский интерфейс поверх русского сада, без обратной дороги: строка уже сохранена. Теперь мир хранит не текст, а замысел — кто это, из чего собран, какой по счёту, — и строка складывается на языке человека в момент показа. Старые сады не пострадали: сохранённые строки читаются как есть.
- **«вчерашний ты» на английском не рождался вовсе.** Самая личная встреча игры — приход себя прежнего — зависела от того, начинается ли реплика со слова «вчера». На английском такой строки не бывает, и орган молчал всю дорогу. Теперь смену характера видно по осям, а не по буквам.
- **двенадцать новых проверок**, испытанных четырьмя подлогами. Всего **98**.

Попутная находка для будущих смен: **новый бросок кубика сдвигает весь мир**. Правка касалась только букв, но два замера баланса покраснели — существа просто стали рождаться другими. Трогая тексты, бери случайность тем же способом и в том же количестве, что и прежний код.

## Прошлая версия: v0.4.45 «сад перестал тараторить»

Игра говорила слишком много, и никто этого не мерил как следует.

- **два вранья в замере.** Болтливость считали по вызовам `G.Voice.say` — но `say` глушит повторы, а `sayText(text, true)` проходит мимо всех кулдаунов: считать надо то, что дошло до экрана. И часы в стендах шли вдвое — `update` двигает `game.time` сам, а стенд прибавлял ещё раз. Все прежние цифры речи были неверны.
- **сад читал стихи поверх стихов**: 154–186 реплик за двадцать минут, больше всех прочих ртов вместе. Рана повторяла «помнит каждый отказ» каждые 1.2 секунды, пока в неё смотришь, — 112 раз за сессию. Аккорд хвалили при каждом созвучии. Все трое утихомирены; действие осталось, повтор ушёл.
- **немых реплик больше нет.** Пулы `idle` и `returner` были написаны давно и не звучали ни разу. Теперь Игра говорит «я ещё здесь» тому, кто замер надолго, и встречает по-человечески того, кто вернулся в третий раз.
- **итог: 12.1 → 7.8 реплик в минуту** в обычной игре, **9.3 → 2.8** для того, кто просто сидит. Пять новых проверок стерегут и болтливость, и обратную крайность: немой Игра быть не должна.

## Прошлая версия: v0.4.44 «рука и тишина»

Две жалобы человека с APK, обе подтвердились замером.

- **гул, растущий со временем**, был утечкой графа: в `audio.js` не было ни одного `disconnect`, отзвучавшие ноты оставались подключены к выходу и продолжали сводиться — 1714 узлов за десять минут. Появился `reap()`: теперь 22 узла на любой минуте при 564 сыгранных нотах.
- **взгляд срывался после перелёта по зову**: срыв мерили расстоянием в мире, а мировая точка под пальцем считается от камеры — камера догоняет игрока, и точка уползает из-под неподвижного пальца. На отставшей камере взгляд рвался за 0.28 с, кристалл на 1.35 с был недостижим. Теперь взгляд держится за точку **экрана**, где палец лёг: сцена вольна уезжать.
- **мелкие не обводились**: захват мерили от центра узла одним радиусом 48, значит малёк требовал целиться на семь точек точнее гнезда — при подушечке пальца в 57 точек. Меряем от края: мелкий прощает 69 точек, крупный 82.
- **новый стенд `aim.js`** поднимает настоящий `engine.js` и держит палец на узле. Три проверки в `run.js`, все испытаны подлогом.

После релиза смена продолжилась — три вещи, которые войдут в следующую сборку:

- **слабый телефон наконец получает обещанное.** `Quality.glow` был написан давно и не читался **никем**: дешёвый телефон рисовал ровно тот же кадр, что и дорогой. Треть кадра — 234 радиальных свечения. Теперь на слабом гаснут два самых массовых украшения: дымка вокруг звёзд памяти (зерно каждой звезды остаётся — небо не редеет) и ореолы цветов сада. Кадр дешевле на **37%**, свет смысла — узлы, существа, игрок, зов — горит везде.
- **сборка APK ушла к GitHub.** Появился `.github/workflows/apk.yml`: каждый пуш в `main` гоняет проверки, сверяет три копии души и собирает APK; тег `v*` сам выкладывает релиз. Плюс `tools/check-sync.sh` — сторож против расхождения `web/` и зеркала.
- **долгая игра промерена.** Стенд `long.js` крутит час игры (216 000 кадров) и снимает мерки на отметках. Мир не пухнет: через час 28 узлов, 160 звёзд, куча под сборщиком 4.2 → 4.4 МБ. Болезнь «растёт со временем» ловится только долгим прогоном — теперь он есть.

## Прошлая версия: v0.4.43 «фон перестал гудеть»

Человек услышал на фоне шум — и был прав дважды.

- **дыхание сада не дышало, а гудело**: LFO качал громкость с размахом 0.45 при уровне слоя 0.04 — вдесятеро громче того, что качает, то есть ровный тон поверх всей игры, даже на пустом берегу. Размах стал долей уровня; пик фона упал с 0.49 до 0.058 при гуле 0.24.
- **шум-погода был почти белым**: полоса Q 0.4 — сипение во всю ширину. Сузил до Q 1.6, пол громкости втрое ниже. Прилив теперь поднимает шум в 12 раз над полом вместо пяти: шум означает событие, а не висит фоном.
- **новый стенд `noise.js`**: подставной Web Audio, который считает громкости слоёв и размах модуляций. Пять проверок в `run.js` стерегут тишину.

## Прошлая версия: v0.4.42 «перерождение говорит»

Продолжение звука — и разбор долга, висевшего несколько смен.

- **перерождение перестало молчать**. Самая крупная потеря в игре: у того, кто только сеет, метаморфоза уносит весь берег до последнего узла, трижды за сессию (замер: 0% выживших против 16–38% у садовника). Игра называла новое имя и ни слова не говорила о том, что сад исчез. Теперь говорит, и по-разному — уцелело или нет.
- **Игра заговорила по-английски целиком**. Имена пород всплывают над каждым рождением, оси подписаны на сигиле — и всё это было только по-русски. Появились `G.kindName` и `G.traitName`; ещё двенадцать видимых строк переехали в словарь. Русский хардкод, висевший списком с прошлых смен, закрыт.
- **проверки стерегут язык**. Обе раскладки одного состава, в английской нет кириллицы, у каждой реплики есть английский пул, у пород и осей — английские имена. Пропажа называется поимённо.

## Прошлая версия: v0.4.41 «у мира прорезался слух»

Человек сказал коротко: «в игре звуки есть, когда с чем-то взаимодействуешь, а на фоне просто шум разного типа». Это оказался точный диагноз. Стенд посчитал, что и как часто звучит за десять минут, — и подтвердил: игра отвечает на прикосновения, но не звучит сама по себе.

- **забвение обрело голос**. Самое тихое место игры: узел тонул в приливе абсолютно беззвучно — за сессию так исчезало до тридцати шести узлов, и человек не слышал ни одного. Теперь уход слышно: падающая нота породы, будто её накрыли водой. Рана звучит иначе — низким скрежетом (этот голос был написан давно, но его никто ни разу не позвал).
- **фон дышит вместе с садом**. Замер показал неприятное: шум держался на одном значении от первой секунды до последней. Берег растёт с одного узла до полусотни, а звучит одинаково — сад было не слышно. Добавлен второй фоновый слой: медленная волна, которая полнеет по мере того, как берег населяется, и глохнет под приливом. Пустой берег молчит.
- **корни звучат всегда, говорят редко**. Укоренение отвечает нотой снизу вверх — опорой, а не вспышкой. Словами Игра сообщает об этом раз в две минуты, а не двенадцать раз за сессию. Заодно вылечен дребезг у порога: корни подтаивали и отрастали, и один узел «укоренялся» трижды.
- **якорь на рождении перестал быть немым**. Тот же жест по живому узлу давал надпись и аккорд, а на новорождённом — ничего: человек не понимал, что удержал.
- **Игра реже называет твою природу**. Реплика доминирующей черты звучала каждые сорок секунд и лидировала в речи — десять-четырнадцать раз за сессию. Теперь раз в две с лишним минуты и только когда черта выражена отчётливо. Самая частая реплика упала с двенадцати до пяти.
- **два новых стенда**. Один считает звуки, второй — речь. Оба спрашивают код сам, а не хранят список: старый замерщик со списком-литералом молча пропускал всё новое. Оба проверены поломками.

Сейчас: 136–211 звуков в минуту, ни одна реплика не звучит чаще раза в две минуты.

## Прошлая версия: v0.4.40 «прилив забирает, голос молчит»

Прошлый релиз («корни») научил заботу окупаться — и перестарался: прилив стал беззубым, забвение съедало всего 2% сада, угроза превратилась в декорацию. Этот релиз возвращает цену бездействию и заодно закрывает рот Игре, которая всё это время тараторила.

- **прилив снова забирает**. Корни без внимания сохнут — полный распад за три минуты. Раньше они не таяли никогда: узел, согретый один раз и брошенный навсегда, оставался бронированным. Теперь забвение съедает около десятой части сада у того, кто только сеет, — и при этом возвращаться по-прежнему вдвое выгоднее, чем бросать.
- **перерождение больше не уносит любимое**. Замер показал жестокое: из двенадцати узлов, к которым человек возвращался чаще всего, метаморфоза забирала **все двенадцать**. Забота была буквально наказуема — выживали как раз те, кого тронули однажды. Теперь глубоко укоренённое переживает перерождение само, даже без якоря.
- **Игра говорит вдвое меньше**. Она приветствовала каждое рождение: две сотни узлов — две сотни реплик, и в этом шуме тонули редкие важные слова. Теперь новую породу встречает один раз, дальше молчит. Фоновые замечания («ты сидишь», «ты бродишь») бросали кубик каждый кадр без всякой паузы — сорок четыре реплики за сессию только про то, что человек сидит. Девяносто реплик за десять минут стало пятьдесят девять, паузы между словами выросли вдвое.
- **у тишины свой голос**. Рождение узла тишины отвечало фоновой фразой «в тебе много несделанного» — упрёк вместо поздравления. Теперь у него три собственные строки.
- **Игра не повторяется дословно**. Шесть реплик существовали в единственном экземпляре и звучали слово в слово каждый раз. Расширены на обоих языках: 151 строка по-русски, 112 по-английски.
- **стенд живого запуска**. Проверки грузили модули логики, но ни разу не поднимали игру целиком — интерфейс и старт не проверялись никогда. Теперь стенд читает `index.html`, поднимает все семнадцать скриптов как браузер, крутит полминуты кадров и растит узел. Сам стенд проверен поломками: сломай сердце игры — он краснеет.

Сейчас: возвращение окупается (35–42% против 17–20% у брошенного), но прилив остаётся голодным — 12% потерь у сеятеля, 9% у садовника.

## Прошлая версия: v0.4.39 «корни»

Этот релиз чинит несправедливость, которую до сих пор никто не замечал: **забота не работала**. Стенд промерил и показал прямо — к чему человек возвращался, выживало 11%, а брошенное 26%. Возвращаться было хуже, чем не возвращаться.

- **корни**. Вернулся к узлу, который потускнел, — он пускает корень. Три возвращения, и узел держится против прилива сам. У корня видны нити вниз, по одной за каждый возврат: считать не нужно, видно и так. Игра говорит об этом один раз — когда узел начал держаться сам.
- **раны заживают**. Раньше урон копился вечно, а одна волна снимала ровно всю жизнь узла: забытое умирало с первого прилива, без второго шанса. Теперь волна забирает не сразу, а в затишье согретое затягивается. Прилив не отменён — он огибает то, во что вложились.
- **якорь переживает перерождение**. Метаморфоза стирала сад подчистую, вместе с якорями: самый дорогой жест игры — долгий неподвижный взгляд — ничего не значил через десять минут. Теперь удержанное переходит в новый мир. Смена кожи, а не амнезия.
- **ничего не исчезает молча**. Прежде при перерождении полторы сотни узлов испарялись без следа — ни звезды, ни счёта. Теперь новый мир помнит весь прежний сад: согретое горит ярче, остальное тише.
- **память узла переживает выход**. Корни, раны, возраст и рост не сохранялись: раненый узел после перезапуска воскресал целым, вложенное внимание обнулялось.
- **небо не копит**. За полчаса игры набегало 565 звёзд и 600 строк — 126 КБ сейва и каша на экране. Теперь небо держит последние 160: старое забвение гаснет, недавнее горит. Сейв — 35 КБ.
- **игра не падает без интерфейса**. Пятнадцать вызовов `G.UI` без защиты — теперь есть немой запасной.

Сейчас: садовник, который держит участок, сохраняет 68–77% сада; тот, кто только сеет и бросает, — 23%. Забвение осталось голодным, но перестало быть слепым.

## Прошлая версия: v0.4.38 «закон виден»

Прошлый релиз («зов», 0.4.37) дал берегу направление. Этот отвечает на третью жалобу второй сессии — **«собирал жёлтенькие молнии непонятно зачем»** — и чинит то, что тихо ломалось годами: память.

- **закон называет себя**. Тронул трещину — Игра говорит имя закона и его следствие: «тяжесть наоборот — шаг идёт в другую сторону». Раньше вспыхивало и гасло.
- **закон видно, пока он живёт**. У временных законов («прилив спит» 32 с, «тяжесть наоборот» 11 с) появился срок: у верхней кромки экрана тает полоска. Мир перестал меняться втайне.
- **закон прощается**. Истёк — берег говорит: «тяжесть вернулась на место». Человек знает, когда снова стал собой.
- **срок переживает выход**. Закрыл игру посреди перевёрнутой тяжести — вернулся в неё же, а не в тихо выпрямившийся мир.
- **память существ вылечена**. Сад, законы, предел якорей, а у существ — характер, истинное имя, признание имени и долг: всё это раньше стиралось при выходе. Существо возвращалось чужим. Теперь возвращается собой.
- **спутник больше не отстаёт**. Привязанное существо (bond > 0.55, страха мало) прибавляет шагу и решимости: отстаёт максимум на 130 вместо 190 и держится в кадре. Непривязанное по-прежнему уходит за горизонт — преданность видна глазом.
- **Игра замечает спутника вслух** — один раз, когда связь родилась.
- **английский берег без кириллицы**. Сигила (берегов / выращено / отпущено / удержано / существ / дорог), сезоны и их подсказки, небо забвения, вехи 5/10/20, реплики про ночь и фронтир — всё переведено. 49 ключей голоса есть в обеих раскладках.

## Стенд проверок

Браузера в песочнице нет, поэтому проверки живут в репозитории:

```
node tools/probe/run.js
```

**77 проверок, 0 падений** (около четырнадцати секунд). Стенд умеет смотреть на узел настоящим жестом (взгляд 1.35 с → кристаллизация), поднимает настоящий `engine.js`, держит палец на узле, слушает звук подставным Web Audio, считает речь, крутит час игры и меряет цену кадра. Он уже поймал недостачи, которые иначе уехали бы в APK: рендер падал без настоящего игрока, `applyLaw` падал без UI, Director спотыкался без ввода, сейв пух до 126 КБ, метаморфоза стирала сад целиком, забота вредила, звуковой граф тёк, а слабому телефону не доставалось обещанного послабления.

Правило смены: **каждая проверка испытывается подлогом.** Сломай то, что она стережёт, — она обязана покраснеть; почини — позеленеть. Проверка, не поймавшая подлог, ничего не проверяет. И у экономии всегда две крайности: не сэкономить и сэкономить всё — сторож стоит с обеих сторон. Добавляешь орган — добавляй проверку.

Рядом два постоянных замера баланса — гонять после любой правки прилива, забвения или заботы:

```
node tools/probe/balance.js   # сколько человек вырастил и сколько у него забрали
node tools/probe/care.js      # окупается ли возвращение к узлу
```

## Что сделать человеку

1. Удалить старую ИГРУ, поставить **0.4.65**, открыть.
2. Играть подольше — **минут десять-пятнадцать**. Именно там раньше начиналась пустота.
3. Главное на эту сборку: **сезон должен идти за твоим характером**. Смени манеру посреди игры — надпись сезона вверху обязана повернуться следом. Раньше она застревала намертво.
4. И финал («отпустить / стать игрой») больше не должен приходить в первые двадцать минут новой жизни.
5. Открывать небо и сигилу как обычно — **теперь это попадает в рассказ честно**. Прошлые нули были моей ошибкой, не твоей.
6. В конце — **сигила → «рассказать» → «скопировать»**.

Ссылка на сборку:
https://github.com/JoTalbot/game/releases/download/v0.4.65/igra-0.4.65.apk
