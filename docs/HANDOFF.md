# Передача смены — читай первым

Работа идёт с разных машин и разными ИИ-агентами. Этот файл — память проекта. Если ты новый агент: прочитай `docs/STATUS.md`, потом это, потом `docs/AGENTS.md`. Не начинай игру с нуля.

**Статус:** 15.08.2026. Релиз **0.4.38** «закон виден» выложен (vc42). После него в `main` легли ещё три шва (APK пока не пересобран): **закон слышно**, **небо не копит**, **корни**. Стенд `node tools/probe/run.js` — **51 проверка**. Ждём ответов человека по 0.4.37/0.4.38.

**Сделано после 0.4.38 (не в APK):**
- **орган «закон слышно»** — пока закон висит, гул берега уведён в сторону (`audio.js: setLaw`, `engine.js` кормит от `world.active`).
- **шов «небо помнит, но не копит»** — `stars` капнуты 160, стихи 60 (`world.addVerse` — единый вход). Сейв за полчаса игры: **126 КБ → 35 КБ**. Немой запасной `G.UI` в `math.js`: игра больше не падает без `ui.js`.
- **шов «корни»** — см. ниже, самое важное изменение баланса за смену.

## Прилив, забвение и корни (15.08, важное)

Замер стендом вскрыл, что **забота вредила**: к чему человек возвращался, выживало 11%, а брошенное — 26%. Причины были три:
1. `hp` только убывал и **не заживал никогда**, а одна волна снимала 1.02 при `hp = 1` — узел умирал с первого прилива.
2. `care` стекает за ~100 с, волна приходит каждые 48–80 с: возврат ничего не укоренял, только подливал то, что всё равно стечёт.
3. `World.metamorphose` объявлял `var keep = []` и **никогда его не заполнял** — перерождение стирало сад подчистую вместе с якорями, а ~180 узлов исчезали молча, мимо неба и мимо счётчика.

Что теперь:
- **Корни** (`node.roots`, 0..1). Растут не от рождения, а от **возвращения**: узел помечается `cooled` при `care < 0.55`, и следующий разогрев выше 0.62 даёт `+0.34` скачком. Три возвращения — узел держится сам. Прилив над укоренённым слабеет (`hp -= dt*0.58*(1 - roots*0.5)`), в затишье раны заживают (`+dt*(0.02 + roots*0.06)`).
- **Корни сохнут без внимания** — `-dt*0.006` при `care < 0.28`, полный распад ~3 минуты. Без этого узел, согретый один раз и брошенный навсегда, оставался бронированным, и забвение не срабатывало ни разу.
- **Метаморфозу переживает укоренённое, а не только якорь**: `anchors.indexOf(n.id) >= 0 || n.roots >= 0.6`. Раньше уносило всё любимое — замер показал, что из 12 узлов с тремя и более возвратами метаморфоза забирала **12 из 12**. Забота была буквально наказуема.
- **Голос:** ключ `rooted` (ru+en), звучит один раз, когда узел начал держаться сам. Видно и без слов: у корня рисуются нити вниз, по одной за возвращение (`renderer.js` в `drawNode`).
- В сейв добавлены `roots`, `cooled`, `hp`, `age`, `growth` у узлов и `carried` у мира. Раньше раненый узел после перезапуска воскресал целым.

**Числа сейчас** (`node tools/probe/run.js`, `care.js`, `balance.js` — гонять все три после любой правки прилива): возвращение окупается — **35–42%** выживания против **17–20%** у брошенного, и при этом прилив остаётся угрозой: забвение съедает **12%** выращенного у сеятеля и **9%** у садовника. Было 87% потерь для всех одинаково.

**Осторожно, три грабли подряд:**
- Ослабишь забвение — укореняется 216 узлов из 217, прилив исчезает как явление. Усилишь — забота перестаёт окупаться.
- **Перебор двух чисел (таяние корней × сила урона) — тупик.** Шесть пар дали потолок 27% против 13% при нужных 15 пунктах разрыва. Помогло только устранение причины: метаморфоза, уносившая всё ухоженное. Если упёрся в потолок — ищи, кто забирает, а не крути коэффициенты. Так же бесплодны броня корней (0.5→0.62) и заживление (0.03→0.06).
- **Слишком быстрое таяние корней (0.015–0.02/с) — ошибка**: корни истлевают за минуту, возврат лишь восполняет потерю, привычка не копится. Таяние должно быть на порядок медленнее роста.
- **Средняя «выживаемость ухоженного» врёт**: её тянут вниз узлы, тронутые однажды. Разбивать по числу возвратов — иначе не увидишь, что самые любимые гибнут поголовно. И мерить характер надо поступками (`_born`/`_returns`), а не состоянием мира: `roots>0.3` у 100% живых узлов при любом стиле игры.

## Голос: сколько Игра говорит (15.08)

Замер: **~90 реплик за десять минут**, медиана паузы 6 с. Тишина важнее фич — стало **~59**, пауза 10 с. Что было не так:
- **Игра приветствовала каждое рождение.** Сеятель делает две сотни узлов и получает две сотни реплик. Теперь породу встречают **один раз** (`Director._greeted`), дальше это просто твоя работа.
- **Фоновые `sit`/`wander` бросали кубик каждый кадр без кулдауна** — отсюда 44 реплики про то, что человек сидит. Троттлинг 60 и 75 с.
- **Рождение узла тишины докладывало фоновым ключом `sit`** («в тебе много несделанного») — упрёк вместо поздравления. У тишины свой ключ `stillBorn`.
- Шесть пулов имели по одной строке и повторялись дословно — расширены в обеих раскладках. Сейчас 151 строка ru / 112 en на 56 ключей, пустых нет.

Проверять замерщиком речи: он обязан звать `Voice.update(dt)`, ставить игровые часы, сбрасывать `Director`/`Voice` и сеять `Math.random` — иначе цифры ложные.

**Релиз 0.4.40** (versionCode 44) собран и выложен: `https://github.com/JoTalbot/game/releases/download/v0.4.40/igra-0.4.40.apk`, id релиза 371056239. Человеку сказано удалить старую и поставить эту.

**Сделано 15.08 (эта смена):** прилив сбалансирован (см. выше), голос притушен вдвое, добавлен стенд живого запуска. Стенды: `run.js` 54/0, `boot.js` зелёный, `care.js` 42% против 20%. Не сделано: `balance.js` показывает, что метаморфоза уносит больше прилива (169–180 против 0–9 у сеятеля) — это, возможно, и правильно, но решить надо осознанно. Остаток русского хардкода: `fate.js` 22/43/100, `organs.js` 198/513/522, `ui.js` hint, `engine.js` 390 и `kindText`, `world.js` «якорь».

**Было 14.08.2026 (ночь):** Сага экрана закрыта (подтверждено). 0.4.34 — свет. 0.4.35 — «дар»: вид→ось на флоутерах, вехи 5/10/20, имя существа всегда + подсказка (Director._nameHinted). Вторая сессия: дошёл до судьбы («стать игрой»), за ним летали двое, но «желтенькие молнии непонятно зачем» (трещины-законы) и «обвёл все круги — ничего не происходит, вышел» (мир в play не рос — scatter звучал лишь при рождении/мете/восстановлении). Ответ — **0.4.36** (versionCode 40, «прилив»): живой фронтир (каждые 3с в observe: unformed<3 в 560px → world.scatter(3, 430) + «дальше опять мерцает. иди.»), закон проговаривает эффект (floater «закон: …» + sayText ru+hint), повторный взгляд по живому узлу: +7 энергии, +0.012 оси узла, кольцо, разовая линия (Director._regazeHinted). Ждём третью сессию: чувство пути, понятность молний. Дальше: баланс прилива-забвения, рты Игры, звук.

## Кто человек

- GitHub: **JoTalbot**, репо **https://github.com/JoTalbot/game** (public)
- Почта для git: `jo.talbot@gmail.com`
- Говорит по-русски. Отвечай по-русски.
- Сказал: «это твоя Игра, командуй мной». Ведёт агент, не человек.
- Играть будет **только с Android APK**, не с браузерного превью. Ритм снимать после его сессий на телефоне.
- Токен GitHub он уже светил в чате. **Не коммитить токены.** Если дали новый — только в remote URL сессии.

## Что это за игра

**ИГРА / IGRA.** Жанр, который мы основали: **рефлектум / онтогенетическая игра**.  
Нет готового жанра. Шесть осей ДНК (взгляд, жар, тишина, тепло, сбой, строй) выращивают органы. Забвение ест то, на что не смотрят. Игра — существо с голосом.

Ядро: `web/` (Canvas + Web Audio). Android — WebView-оболочка. Одна душа.

## Где что лежит

| путь | смысл |
|---|---|
| `web/` | источник правды игры |
| `android/` | оболочка. Java `MainActivity`, без AndroidX |
| `tools/build-apk.sh` | сборка APK без Gradle (на 2 ГБ RAM Gradle дохнет) |
| `dist/igra-*.apk` | готовые сборки |
| `docs/play/` | копия web для GitHub Pages |
| `docs/STORE.md` `PRIVACY.md` `TRAILER.md` | витрина |
| `docs/AGENTS.md` | закон режиссёра |

Страницы: https://jotalbot.github.io/game/  
Релизы: https://github.com/JoTalbot/game/releases

## Как собрать APK

SDK и JDK 17 в `/home/user/.igra-build/`.

```bash
export JAVA_HOME=/home/user/.igra-build/jdk-17
export PATH="$JAVA_HOME/bin:$PATH"
export ANDROID_SDK_ROOT=/home/user/.igra-build/android-sdk
bash tools/build-apk.sh
```

Скрипт копирует `web/` → `android/app/src/main/assets/www`, пакует aapt2+d8+apksigner.  
Keystore: `dist/debug.keystore` (gitignored). Если его нет — скрипт создаст **новый**, и человек **обязан удалить** старую ИГРУ с телефона, иначе «приложение не установлено».

## История починки экрана Android

1. 0.4.0–0.4.3: масштабирование WebView и растягивание при повторном открытии давали белое поле или сжатие.
2. 0.4.4: попытка зашить физические пиксели через JS (`applyPhysicalViewport`) и `setInitialScale(100)` сжало вьюпорт ровно в 1.5–3 раза (DPR) в левый верхний угол (384×708 на экране 576×1280).
3. **0.4.5 (настоящее решение):** убран весь костыльный JS-инжект вьюпорта из Java. WebView работает в нативном полноэкранном режиме с аппаратным ускорением (`Theme.Black.NoTitleBar.Fullscreen`, `WindowInsetsController` hide bars). HTML/CSS закреплён в `position: fixed; inset: 0; width: 100%; height: 100%`. В `engine.js` холст берет реальный `innerWidth/innerHeight` и умножает на `dpr` с матрицей `ctx.setTransform(dpr, 0, 0, dpr, 0, 0)`. В цикле кадров и на событиях resize/orientation изменения размера подхватываются на лету.

## Закон экрана (истина, добытая 0.4.22 → 0.4.33)

Симптом саги: берег занимает левую верхнюю часть экрана, справа и снизу «мягкий двойник» из старых кадров. Соотношение следует за плотностью прошивки (2/3 при dpr 1.5, 0.74 при dpr 1.35).

**Настоящий корень — в нашем коде, не в прошивке.** `renderer.js` начинал кадр с `ctx.setTransform(identity)` и заливал фон `fillRect(0,0,w,h)` — в битмапе размером `w×dpr` закрашивались только (w,h) пикселей. Видимая часть = 1/dpr битмапа; остальное — несмываемый мусор мёртвых кадров. DOM, увы, всегда рисовался честно ×dpr — отсюда «титул на весь экран», кнопки HUD за границей берега и точные касания. Метки краёв (зелёная полоса на правой кромке мира) доказали здоровье композитора: она встала в физический край экрана.

**Закон холста, навсегда:**
1. Битмап = `innerWidth × dpr`; трансформ — `setTransform(dpr,…)`; система координат мира — CSS-пиксели. НИКОГДА не сбрасывать трансформ в identity для заливки/кадра (исключение — только точечный readPixels-подобный код). Везде, где пишется `setTransform`, подставлять `game.dpr` (учитывает Quality-кепку), а не сырой `devicePixelRatio`.
2. Оболочка: позднее рождение WebView из `decorView.post()`, MATCH_PARENT, `?debug` виде́н только в диагностических сборках.
3. Диагностика делом: метки краёв в кадре (один скрин равен десяти теориям), зонд касаний — сторож, телеметрия — по `?debug`.

**Кладбище гипотез (не воскрешать):** «физический вьюпорт» meta из Java; setInitialScale/плотность; setScaleX/Y матрица view; перепин LayoutParams; GONE→VISIBLE; sw-сборка (LAYER_TYPE_SOFTWARE); willReadFrequently; manifest hardwareAccelerated=false; «обнови WebView». Ни одно не лечило, потому что лечить надо было `setTransform`.

Проверка настоящая только на телефоне человека. Превью врёт. После зелёной сессии убрать `#fit-debug` из релиза (писать только при `?debug`) — перед этим дождаться скрина с телеметрией: измеренная геометра окна G1 навсегда снимает гадания.

## Стенд без браузера (`tools/probe/`)

Браузера в песочнице нет, а орган надо проверить до APK. `tools/probe/harness.js` грузит `web/js/*.js` в node с подставными `window/document/localStorage` и даёт `boot() / makeWorld() / step() / ctxStub()`. `tools/probe/run.js` — набор проверок:

```
node tools/probe/run.js
```

Проверки сторожат то, что **уже ломалось**: зов зовёт снова, компас появляется, текст зова не заморожен в сейве, спутник не отстаёт, сад/законы/лица существ переживают выход, кадр начинается с матрицы плотности (закон холста), Игра не тараторит. Добавляя орган — дописывай туда проверку, это дешевле, чем гонять человека ставить APK.

Важно: подставной игрок в стенде — настоящий `new G.Player()`, иначе рендер падает на `p.trail`. Стенд это уже ловил.

**`node tools/probe/boot.js` — живой запуск.** Остальные стенды грузят только модули логики и ни разу не поднимают игру целиком: `ui.js`, `Lang.apply` и `boot()` из `main.js` ими не проверялись никогда. Этот читает `index.html`, грузит все 17 скриптов в его порядке с минимальным DOM, поднимает `G.app`, крутит 1800 кадров и кристаллизует узел.

**Урок про стенды-пустышки:** первая версия `boot.js` оставалась зелёной, когда `crystallize` переименовали в `crystallizeTYPO` — потому что она ничего не играла, только проверяла, что скрипты прочитались. Стенд, который не может упасть, бесполезен. **Проверяй новый стенд поломкой:** сломай то, что он сторожит, убедись, что он покраснел, и только потом верь зелёному.

Подписи, на которых легко ошибиться (стенд ловил обе): `world.update(dt, player, dna, fx, game)` и `world.crystallize(node, gest, dna)`.

## Урок про `.git` (сюрприз 15.08)

`.git` **не пережил снапшот** между сессиями: рабочие файлы на месте, история исчезла (`fatal: not a git repository`). Лечение без потери правок: склонировать репозиторий во временную папку и перенести оттуда только `.git`:

```
cd /tmp && git clone https://<user>:<token>@github.com/JoTalbot/game.git gitrec
cp -a /tmp/gitrec/.git /home/user/game/.git
cd /home/user/game && git config user.name JoTalbot && git config user.email jo.talbot@gmail.com
```

Незакоммиченные правки при этом видны как обычные изменения — ничего не теряется. Отсюда правило: **коммить и пушить мелкими кусками**, не копи работу в рабочей копии.

## Урок снапшотов этой машины (боль на раз)

Снапшот рабочего места **не поднимает** `.igra-build/` (~600 МБ > лимита): JDK, SDK и `dist/debug.keystore` умирают между сессиями. Каждая новая сессия = цех восстанавливать цепью из команд (JDK 17 Temurin → cmdline-tools → sdkmanager platform-34+build-tools) и **подпись каждый раз новая — предупреждать человека об удалении старой ИГРЫ в каждом STATUS**. `.git/config` в снапшот тоже не входит: origin с токеном пересоздавать в каждой сессии перед пушем; и `git fetch` до коммита — локальная история может отставать от origin.

## SDK и подпись на текущей машине

SDK восстановлен по рецепту выше (JDK 17 Temurin + cmdline-tools, sdkmanager ставит platforms;android-34 и build-tools;34.0.0). В скрипте javac вызывается с `-encoding UTF-8` — не убирать (комментарии по-русски). **14.08 создан новый `dist/debug.keystore`: подпись сменилась — человек обязан удалить старую ИГРУ перед установкой 0.4.23+.** Keystore снапшот не хранит: на новой машине подпись снова будет другой, предупреждай человека в STATUS.

## Что человек уже играл

- Поставился, родился, берег живой.
- Сезон на скрине: **странствие**. Значит, ДНК ушёл во взгляд/curiosity.
- Видел стих «пауза имеет форму», аккорд, голос «ты попал в мой пульс».
- Кнопка «родиться» работает.

## Орган «зов» (0.4.37) — как он устроен

Родился как ответ на вторую половину жалобы: «обвёл все кружки — ничего не происходит». 0.4.36 вернул мерцание (мир не кончается), но не вернул **направление**. Зов — направление без карты.

Живёт целиком в `web/js/world.js`: поля `call/callT/arrived` в конструкторе, `makeCall` / `updateCall` / `arrive`. Дёргается из `World.update` (в `engine.js` в вызов добавлен пятый аргумент `this` — игра нужна для fx/floaters). Рисуется `Renderer.drawCall` (`renderer.js`): на экране — маяк с подписью, за экраном — стрелка у кромки плюс расстояние. Кладётся в сейв (`toJSON` + разбор в `Game.load`), обнуляется в метаморфозе. Счётчик «дорог» — в сигиле (`ui.js`).

Что важно не сломать при правках:

- **Зов — не квест.** Не давать список целей, не показывать маршрут, не наказывать за игнор. Стрелка тянет, но пропустить зов ничего не стоит: он просто сменится.
- **Тексты живут в `world.js` в словарях `CALL_TEXT/CALL_OPEN/CALL_ARRIVE/CALL_MARK` с ключами `ru`/`en`** и функцией `G.callText(trait)`. Рендер зовёт `G.callText`, а не хранит строку в объекте — иначе переключение языка на лету оставит старый текст (и старый сейв не сломает: `c.text` больше не сохраняется).
- Природа зова = доминантная ось ДНК на момент рождения зова, поэтому даль всегда «про тебя»: агрессивному — сопротивление, тихому — тишина.
- Награда за приход — **мир, а не очки**: гнездо узлов нужной природы + scatter + кормёжка оси. Никаких валют.
- Пауза между зовами 20–40 с (`callT`), первый — на 30-й секунде мира. Не убыстрять: зов не должен превращаться в конвейер.

Проверялось без браузера: headless-прогон через `node` с подставными `window/document` (грузит `web/js/*.js` в `vm`), три прихода за ~130 с симуляции при скорости игрока ~85/с; отдельный прогон `Renderer.drawCall` со stub-контекстом — обе ветки (маяк и компас) рисуют. Приём рабочий, повторять при следующих органах: браузера в песочнице нет.

## Орган «закон виден» (0.4.38) — как он устроен

Ответ на третью жалобу: «собирал жёлтенькие молнии непонятно зачем». Молния (трещина) давала эффект, но человек не знал ни имени эффекта, ни срока — мир менялся втайне.

- У законов в `organs.js` (`LAWS`) появились поля `en`/`enHint` и **`lasts`** — срок в секундах у тех, кто временный (`tideSleep` 32, `invert` 11). `lawName()/lawHint()` выбирают раскладку.
- `applyLaw` кладёт временный закон в **`world.active`** (`{id, left, full}`). `World.update` тикает `left`, а на нуле роняет закон и говорит `G.lawEnded(id)` (словарь `LAW_END` в `world.js`, ru/en).
- Рисуется в `renderer.js` (после частиц, до вспышки метаморфозы): полоска у верхней кромки, длина = доля оставшегося срока. Не текст — текст уже сказан голосом, на экране остаётся только «сколько ещё».
- **В сейве**: `active`, `tideFrozen`, `invertMove` в `World.toJSON` и в `Game.load`. Иначе мир тайком выпрямлялся, пока человека нет.
- `applyLaw` больше не требует `G.UI` (проверка `if (G.UI && G.UI.law)`) — это нашёл стенд, но то же самое падало бы при раннем законе до готовности UI.

## Спутник (0.4.38)

`updateBeings` в `world.js`: `companion = bond > 0.55 && fear < 0.5`. Дальше 100 — прибавка скорости `58 + min(80,(d-100)*0.7) + bond*30` и флаг `b.hurry` (рендер провисает нить сильнее). Дальше 110 — намерение растёт до `min(1, 0.8+(d-110)/200)`: преданность пересиливает застенчивость характера. Замеры стенда: преданный отстаёт на 130–137, непривязанный уходит на 2724. Порог 0.55 переходится один раз (`b.saidCompanion`) — тогда Игра говорит `G.companionLine()`.

## Язык: правило двух раскладок

Проверено стендом и закреплено проверками: **49 ключей голоса существуют и в `LINES` (`igra.js`), и в `G.LINES_EN` (`lang.js`)**; в `G.UI_STR.en` нет ни одной кириллической буквы. Появился `G.Voice.keys()` — им стенд сверяет полноту.

Что было зашито по-русски и переехало в словари: небо забвения (`skyLine`), подписи сигилы (`statShores/statGrown/statLost/statHeld/statBeings/statRoads`), слова «сезон»/«день», вехи 5/10/20 (`ms5/ms10/ms20`), ночной отчёт (`nightLost/nightBloom`), подсказка про имя (`nameHint`), фронтир (`frontier`). Сезоны получили `en`/`enHint`, а `G.Memory` — `climateName()/climateHint()`; **`id` сезона остаётся русским ключом** (он в сейве и в коде), переводится только показ.

## Закон продукта

Нет доната, рекламы, энергии за деньги. Оффлайн-первый. Локальный Director. Облачный рот — опциональный webhook в сигиле, не костыль.

Пуш в `main` сразу, как кусок дышит. Коммиты по-русски, коротко.

## Когда вернёшься к геймплею

Спросить / дождаться от человека три вещи с живой сессии:

1. как Игра его назвала  
2. что выросло первым  
3. в какой момент захотелось закрыть  

После 0.4.37 добавились ещё два: **тянет ли стрелка зова** и **что сказали молнии** (законы). Спутник уже вырос (0.4.38) — существа идут за тобой; спросить и про него: **заметил ли, что кто-то не отстаёт**.

Пока ответов нет, работа идёт по внутреннему долгу: память (сделано), понятность законов (сделано), полнота второй раскладки (сделано), стенд проверок (растёт). Следующее на очереди без человека: звук (`audio.js` беднее всего остального), баланс прилива-забвения, рты Игры.

## Звук (0.4.41) — что уже слышно и чего ещё нет

Человек описал звук так: «есть, когда с чем-то взаимодействуешь, а на фоне просто шум разного типа». Это был диагноз, и он подтвердился замером.

**Стенд `tools/probe/sound.js`** считает, что звучит за 10 минут в режимах «сеятель»/«садовник». Он спрашивает список голосов у самого `audio.js` (`Object.keys(G.Audio)` минус служебные) — **не вбивай список руками**: прошлая версия со списком-литералом молча пропускала новые звуки, а на добавленных `forget`/`rooted` вообще упала, провалившись в настоящий Web Audio, которого в node нет.

**Стенд `tools/probe/voice.js`** считает речь. Важно: считать надо **прозвучавшее, а не вызовы `say`** — признак «сказала» это обновившийся `Voice.lastAt`. Счёт всех вызовов даёт `lost×36` там, где человек слышал реплику дважды. Стенд краснеет, если одна реплика звучит чаще раза в 40 с.

**Осторожно с замерами взгляда.** В `engine.js`:654 `gazeTick` звучит шесть раз в секунду (`Math.floor(gazeT*6) !== Math.floor((gazeT-dt)*6)`), а не каждый кадр. `harness.js` сначала повторял его без этого условия и насчитал 17 тысяч тиков вместо 1500 — я чуть не пошёл «чинить» несуществующий звуковой мусор. Любой стенд, повторяющий логику движка, обязан повторять её точно.

Что сейчас звучит за сессию (садовник, 600 с): `gazeTick×750, speak×470, crystallize×107, rooted×24, forget×8, tide×5`. Не звучат ни разу и это нормально: `beat` (только ритуал рождения), `pulse` (редкое событие Директора), `ui`, `metamorphosis` (зовётся из `engine.js`/`fate.js`, стенд их не крутит), `tone`/`chord`/`pluck` (органы и резонанс).

**Фон.** Было: один шумовой слой, `gain` 0.011 от первой секунды до последней — берег растёт с 1 узла до 53, а звучит одинаково. Стало: второй слой `garden` (`_buildGarden`) — две расстроенные квинты 196/294.3 Гц под lowpass, дыхание LFO 0.05 Гц, громкость от числа **живых** узлов (насыщение на 50) плюс добавка за укоренённые (на 8), lowpass падает с приливом. `Audio.update` теперь принимает пятым аргументом `world`.

**Правило, которое стоит держать:** звук — на каждое событие, слова — редко. Укоренение звучит все 24 раза за сессию, а говорит о нём Игра раз в две минуты (`world._rootSaid`). То же различие спасло голос породы: реплика доминирующей черты лидировала в речи (10–14 раз), кулдаун 42 → 135 с и порог 0.22 → 0.3.

**`n.rootTold`** — «об этом узле уже объявляли», лежит в сейве (`World.toJSON` + `Game.load`). Без него корни дребезжат у порога 0.6 (тают и отрастают) и один узел «укореняется» трижды: замер ловил 29 объявлений на 24 узла.

Осталось неразобранным: метаморфоза уносит 138–151 узел против 26–36 у прилива — перерождение остаётся главным пожирателем сада, и это до сих пор не осмыслено как замысел. Русский хардкод: `fate.js`:22/43/100, `organs.js`:198/513/522, `ui.js` hint, `engine.js`:390 и `kindText` (слово «якорь» уже переехало в `UI_STR.anchor`).

## Язык: что закрыто в 0.4.42

Русский хардкод, висевший списком с прошлых смен, разобран. Появились **`G.kindName(kind)`** (`world.js`, рядом с `KIND_RU`/`KIND_EN`) и **`G.traitName(trait)`** (`dna.js`, рядом с `TRAIT_RU`/`TRAIT_EN`) — звать надо их, а не словари напрямую. Имя породы всплывает над каждым рождением, оси подписаны на сигиле: это самые частые надписи в игре, и до сих пор они были только по-русски. Английские имена держат ту же меру, что русские: `still` → `stillness`, `curiosity` → `gaze`, `chaos` → `glitch` — одно образное слово, не термин.

Двенадцать видимых строк переехали в `UI_STR` (`bossGathered`, `skyEmpty`, `skyTouch`, `shoreBreathes`, `refuseMem`, `aliveWarmth`, `seamHit`, `shardOf`, `wasAbandoned`, `whatYouLeft`, `yesterdayYou`, `hintBirth2`, `anchor`). В `fate.js` хардкода не было — там развилки `G.Lang.id === "en"`, это законно.

**`boot.js` теперь стережёт язык:** обе раскладки `UI_STR` одного состава, в `en` нет кириллицы, у каждой реплики голоса есть английский пул, у всех пород и осей есть английские имена. Проверки называют пропажу поимённо (`нет в en: skyTouch`).

**Не вбивай в проверки числа.** Стояло `Voice.keys().length === 56` — оно краснело на каждой новой реплике, ничего не сообщая о качестве, и я потратил ход на разбор ложной тревоги. Проверять надо свойство (полнота раскладок), а не количество.

## Шум на фоне: как нашёлся и чем лечится

Человек с APK: «гораздо лучше, но всё равно на фоне какой-то шум». Виноваты были две вещи, обе — мои же прошлые правки.

**1. Дыхание сада не дышало, а гудело.** В `_buildGarden` LFO подмешивался в громкость слоя с размахом **0.45**, тогда как сам слой держится на **0.04**. Модуляция была вдесятеро громче того, что модулирует: вместо медленного набухания получался ровный тон 196+294 Гц поверх всей игры, и на пустом берегу тоже — уровень 0, а качает всё равно. Лечится тем, что размах обязан быть долей уровня: `amt.gain = lvl * 0.34` в `update`. Пик фона упал с 0.49 до 0.058, при гуле 0.24.

**2. Шум-погода был почти белым.** Полоса `Q 0.4` — это не полоса, а сипение во всю ширину. Сузил до `Q 1.6`, центр 300 Гц, пол громкости 0.012 → **0.004**. Теперь прилив поднимает шум **в 12 раз** над полом (было впятеро): шум стал означать событие, а не висеть фоном.

**Урок про Web Audio:** громкость параметра — это НЕ то, что записано в `gain.value`. Всё, что подключено в параметр (LFO, другой гейн), складывается с ним. Задавая модуляцию числом, всегда сверяй его с амплитудой того, что качаешь.

**Стенд `tools/probe/noise.js`** — подставной Web Audio, который не звучит, а считает: постоянные уровни слоёв, размах модуляций (с учётом того, что влито в параметр), плотность коротких нот. Работает и сам (`node tools/probe/noise.js` — печатает картину покоя и полного берега), и как библиотека (`measure(сек, "idle"|"gaze")`) для пяти проверок в `run.js` («на пустом берегу сад молчит», «дыхание тише того, что дышит», «фон не перекрикивает гул», «в покое шума почти нет», «прилив слышно как событие»). Проверено подлогом: возврат 0.45 красит три из пяти.

**Важно:** `Audio.update` зовёт движок (`engine.js`:535), а движка в стенде нет. Замер без ручного вызова показывал нулевой уровень сада и молчал о болезни. То же с пылинками (`engine.js`:515) — стенд обязан повторять строки движка, иначе меряет пустоту. Заодно проверил глаз: частиц в среднем 46 из 200, визуал чист.

