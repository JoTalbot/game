# RC1 — improvement backlog

Дата: 3 сентября 2026.

Этот backlog содержит улучшения, найденные на реальных прогонах RC-подготовки. Продуктовые изменения сначала фиксируются здесь, затем реализуются после подтверждения данных.

## IMP-RC1-TOUCH — adaptive touch cancellation

**Приоритет:** P1 / RC polish  
**Статус:** подтверждено двумя независимыми реальными сессиями; принято к реализации.

### Evidence

Сессия 1, `v2.33`, экран `427×948`, слабый профиль:

- 10 срывов;
- 3 из них — уход пальца;
- медиана ухода `128 px`;
- максимум `139 px`;
- действующий порог `126 px`.

Сессия 2, тот же физический класс устройства:

- 27 срывов;
- 5 из них — уход пальца;
- медиана ухода `132 px`;
- максимум `174 px`;
- действующий порог `126 px`;
- 20 срывов были явным отпусканием, поэтому порог нельзя просто поднять до 140 px.

Итого: `8` finger-left событий в двух независимых сессиях. Это достаточно для принятия hysteresis-кандидата, но не для окончательной фиксации баланса.

### Решение

Ввести hysteresis/grace zone:

- `0–126 px` — обычное удержание;
- `126–140 px` — временная зона допуска;
- возврат пальца внутрь grace zone не рвёт удержание;
- устойчивый выход `>140 px` — срыв `slip`;
- явный `touchend` остаётся обычным отпусканием;
- системный `touchcancel` остаётся отдельным системным событием;
- не менять стоимость взгляда, рождение, пульс или правила мира.

`140 px` — стартовая консервативная граница для физической проверки, а не окончательный gameplay-баланс.

### Acceptance

- deterministic probe покрывает `126–140` с возвратом;
- deterministic probe покрывает `>140` с отменой;
- explicit release не меняется;
- `touchcancel` не превращается в `slip`;
- существующие gesture probes остаются зелёными;
- на реальном слабом Android нет роста ложного удержания соседних узлов.

Evidence: `docs/RC1_TOUCH_EVIDENCE.md`.

## IMP-RC1-COLOR — color discoverability

**Приоритет:** P2 / research  
**Статус:** наблюдение, без реализации.

Первая сессия дала `0` цветов, вторая `2`. Этого всё ещё недостаточно для вывода о проблеме UX.

### Следующее измерение

Собрать несколько независимых паспортов и сравнить:

- количество цветов;
- контекст, в котором игрок их обнаруживает;
- длительность до первого цветового взаимодействия;
- связь с природой/законами/органами;
- наличие цветового результата без внешней подсказки.

Tutorial или UI-подсказку не добавлять только ради увеличения метрики.

## Правило RC

После принятия improvement сначала реализуем его и прогоняем полный probe/CI. Затем повторяем физический smoke. Production release остаётся закрыт до прохождения физического RC gate.
