var IGRA = IGRA || {};
(function (G) {
  "use strict";

  // Версия видна в отчёте с телефона: без неё нельзя понять, о какой
  // сборке говорит человек. Сверяется сторожем `tools/check-sync.sh`
  // вместе с build-apk.sh и gradle — три места, одно число.
  G.VERSION = "3.0.0-rc1";

  // Отладочный режим для тестов: ускоряет развилку судьбы, чтобы человек
  // на устройстве мог быстро дойти до конца и проверить обе концовки
  // (отпустить / стать игрой). Включается пятью быстрыми касаниями по
  // логотипу «ИГРА» на титуле, живёт в сейве (igra.debug.fast), обычному
  // игроку не виден и не мешает — порог судьбы он не трогает.
  G.DEBUG = { fast: false };