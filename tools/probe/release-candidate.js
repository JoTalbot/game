#!/usr/bin/env node
const fs = require("fs");
const vm = require("vm");
const path = require("path");
const root = path.resolve(__dirname, "../..");
const html = fs.readFileSync(path.join(root, "web/index.html"), "utf8");
const files = [...html.matchAll(/<script[^>]+src=["']([^"']+)["']/g)].map(m => m[1]).filter(Boolean);
const required = ["js/release-systems.js"];
const ok = (v, label) => { if (!v) throw new Error(label); console.log(`✓ ${label}`); };
const noop = new Proxy({}, { get(t, p) { if (p === "measureText") return () => ({ width: 0 }); if (!(p in t)) t[p] = () => {}; return t[p]; }, set(t, p, v) { t[p] = v; return true; } });
const canvas = { style: {}, width: 800, height: 600, offsetWidth: 800, offsetHeight: 600, getContext: () => noop, getBoundingClientRect: () => ({left:0,top:0,width:800,height:600}), addEventListener() {} };
const ls = { _d: Object.create(null), getItem(k){return this._d[k] ?? null}, setItem(k,v){this._d[k]=String(v)}, removeItem(k){delete this._d[k]}, clear(){this._d=Object.create(null)} };
const context = { console, setTimeout, clearTimeout, setInterval, clearInterval, requestAnimationFrame:()=>0, cancelAnimationFrame:()=>{}, performance:{now:()=>Date.now()}, location:{href:"https://igra.local/",protocol:"https:",search:""}, navigator:{language:"ru-RU"}, localStorage:ls, document:{readyState:"complete",documentElement:{lang:"ru",clientWidth:800,clientHeight:600},body:{appendChild(){},removeChild(){},classList:{add(){},remove(){},contains(){return false}}},createElement(){return {style:{},setAttribute(){},appendChild(){},remove(){},addEventListener(){}}},getElementById(id){return id==="stage"?canvas:null},querySelector(){return null},querySelectorAll(){return []},addEventListener(){}}, window:null, globalThis:null, innerWidth:800,innerHeight:600,devicePixelRatio:1,screen:{width:800,height:600},AudioContext:function(){},webkitAudioContext:function(){}};
context.window=context; context.globalThis=context; context.addEventListener=()=>{}; context.removeEventListener=()=>{};
const sandbox=vm.createContext(context);
for(const src of files) vm.runInContext(fs.readFileSync(path.join(root,"web",src),"utf8"),sandbox,{filename:src});
const G=sandbox.IGRA;
ok(!!G.ReleaseSystems,"ReleaseSystems loaded");
ok(required.every(x=>files.includes(x)),"release module is in browser shell");
G.Save.clear(); G.ReleaseSystems.reset();
const s=G.ReleaseSystems.state();
ok(s.places.length===8,"8 historical places seeded");
ok(s.beings.length===6,"6 recurring identities seeded");
ok(s.laws.length===0,"laws start emergent, not hard-coded UI");
const game={state:"play",time:2400,player:{x:0,y:0},dna:{age:950,dominant:()=>"empathy"},world:{nodes:[{x:0,y:0,r:20,care:1,dead:false}],beings:[],wounds:[],meta:3,discovered:100,lost:0,bounds:2200,scatter(){},nearestNode(){return this.nodes[0];}}};
for(let i=0;i<420;i++) G.ReleaseSystems.observe(1,game);
let p=G.ReleaseSystems.profile();
ok(p.act>=3,"third act reached");
ok(p.places.length===8,"place history remains bounded");
ok(p.beings.length===6,"being history remains bounded");
ok(p.events.length<=32 && p.causes.length<=96,"causal/event history is bounded");
ok(p.rare.length>=3,"rare personal events emerge from life signals");
G.ReleaseSystems.recordAction("harm","player","scar",{severity:2},game);
G.ReleaseSystems.recordAction("care","player","root",{severity:2},game);
p=G.ReleaseSystems.profile();
ok(p.causes.some(c=>c.type==="harm") && p.causes.some(c=>c.type==="care"),"actions carry provenance");
ok(p.events.some(e=>e.causeId),"events reference causeId");
const before=JSON.stringify(p);
G.ReleaseSystems.chooseFinale("release",game);
G.ReleaseSystems.chooseFinale("become",game);
p=G.ReleaseSystems.profile();
ok(new Set(p.finals.map(f=>f.choice)).size>=2,"release and become produce distinct final traces");
ok(p.nextLife.generation>=2,"next-life generation persists");
ok(p.nextLife.inherited.length>=3,"next life inherits causal state");
G.ReleaseSystems.migrate({version:0,world:{discovered:2},dna:{age:100}});
p=G.ReleaseSystems.profile();
ok(p.body.habits.length>0,"legacy save migration envelope works");
const valid=G.ReleaseSystems.validate();
ok(valid.bounded && valid.serializable && valid.causal,"state validates and serializes");
console.log(`RELEASE CANDIDATE GATE ${valid.ready ? "READY" : "INTEGRATION READY"}`);
